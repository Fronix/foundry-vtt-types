# Phase 5 — Canvas (`client/canvas`) — detail

> Per-phase detail for the v14 migration. Slim tracker: [migration-v14.md](migration-v14.md). Closed phases: [migration-v14-archive.md](migration-v14-archive.md).

## Phase 5 (ACTIVE) — detail

**Scope.** The `client/canvas` layer: (1) **verify** the existing canvas `.d.mts` files member-by-member against their v14 `.mjs` (same done-bar recipe as Phase 4 — diff class bodies, fix drift, member order matches source); (2) **add the 20 missing canvas files** (ex-vfx — see list below); (3) there are **0 canvas stub files** (all 65 stubs are in `applications/`, → Phase 6). The `canvas/vfx/` subsystem (~21 files) is intentionally **excluded** — it is Phase 7.

**Ground truth:** `/home/fronix/git/foundry/resources/app/client/canvas/<path>.mjs` (v14.363.0) ↔ `src/foundry/client/canvas/<path>.d.mts`.

**Batch plan (foundation-up).** 207 verify + 20 add is multi-session; batched like Phase 4. Expand each batch into per-file detail when reached; prune closed-batch detail (→ archive) to keep this section small. Survey refreshed 2026-05-26 (counts below; verify each file against source before editing — counts are guidance).

| Batch    | Scope (verify N existing + add M new)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Status                                                                                                                                                                                                                                                                                                                                           |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **5.1**  | **geometry/** — verify 11; rename `quad-tree`→`quadtree`; add `shapes/elevated-surface-exposure-polygon`. **Wall EDGE deferral DONE.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **DONE — CI green** (Level-entangled bits → P7; see below)                                                                                                                                                                                                                                                                                       |
| **5.2**  | **rendering/** — verified ~89 (shaders/\* incl `lighting/effects` 26, `filters` 13 + `smaa` 4, samplers, weather, vision, region, grid, graphics, `batching` 2, `mixins` 2). 0 new. Landed the v14 shader/filter base-API refactor (coupled across ~60 files, atomic) — see Batch 5.2 detail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **DONE — CI green**                                                                                                                                                                                                                                                                                                                              |
| **5.3**  | **sources/ (11) + perception/ (10)** — light/vision/sound/movement sources + detection-modes + fog/vision-mode/perception-manager. Added missing `sources/_types.d.mts`. Pure member-diff (no shader-API coupling); Scene-Levels bits FIXME'd → P7. See Batch 5.3 detail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **DONE — CI green**                                                                                                                                                                                                                                                                                                                              |
| **5.4**  | **containers/ (17) + primary/ (6)** — added `primary/primary-canvas-particle-container`. (`containers/elements/shape-controls` stays in the 5.6 shape-controls cluster — needs `client/data/shapes.d.mts`.) Marked 3 classes `@deprecated since v14` (cascaded disables to 5.5/5.6/globals — see detail). No shader-API coupling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **DONE — CI green**                                                                                                                                                                                                                                                                                                                              |
| **5.5a** | **groups/ (9 + barrel) + layers/base/ (3)** — the foundation. Groups verified (BaseCanvasMixin/darknessPenalty/effects-trio/mapElevationToDepth removed; primary `levelTextures`/`hoverFadeElevation→deprecated`; visibility `surfaceExposure`/array `testVisibility`). layers/base: canvas-layer (`getZIndex`/`zIndex` moved down), interaction-layer (**now abstract** + TOGGLE_PALETTE/prepareSceneControls/keypress family), placeables-layer (v14 rewrite: `clipboard`, `_configPreview`, `paletteCreateData`, `viewedDocuments`, undo/keyboard/copy family, deprecations). See Batch 5.5a detail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **DONE — CI green**                                                                                                                                                                                                                                                                                                                              |
| **5.5b** | **layers leaf (NON-ShapeLayerMixin) + effects/ + masks/ + barrels** — controls, grid, notes, templates(@deprecated), tokens, walls, effects/\* (5), masks/\* (3), `_module`, `_types`. Each needs FIXMEs for: `paletteClass` (5.6), `prepareSceneControls` (type **exists** — `SceneControls.Control`), token-movement (P7), Scene-Levels (P7), `ParticleGenerator` (5.7), `PolygonTree`/`RegionSurface` (P7).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **DONE — CI green**                                                                                                                                                                                                                                                                                                                              |
| **5.6**  | **placeables/ (22) + the "shape controls" cluster + the 5 ShapeLayerMixin leaf layers.** Decomposed foundation-up into **5.6a–5.6g** (it's ~15.5k lines of placeable source + a 2904-line `client/data/shapes.d.mts`): **5.6a** placeable-object base **(DONE — CI green)** · **5.6b** non-shape leaf placeables (note, wall, template + regions/{geometry,mesh}) · **5.6c** _(DONE)_ tokens/ subdir + token placeable non-movement surface (5861-line source; token-movement/drag → P7) · **5.6d** add `client/data/shapes.d.mts` (10 ShapeData classes + `ClientShapeDataMixin`; Scene `_dependentShapes`/PolygonTree FIXME→P7) + `placeables/mixins/shapes` (`ShapeObjectMixin`) + shape-controls cluster + `containers/elements/shape-controls` (def. from 5.4) · **5.6e** shape leaf placeables (drawing, light, sound, tile, region) + add deferred `shape`/`_shape` derived-props on docs · **5.6f** `layers/mixins/shapes` (**ShapeLayerMixin**) + the 5 `ShapeLayerMixin(PlaceablesLayer)` leaf layers (drawings/lighting/sounds/regions/tiles, reassigned from 5.5) · **5.6g** palette app classes in `applications/sheets/palette/` (resolve `paletteClass`/`paletteCreateData` FIXMEs). | **5.6a–5.6f DONE & CI-green** (5.6f: `ShapeLayerMixin` + `ShapeLayerPlaceablesLayer` intermediate + 5 reparented leaf layers). (5.6c token placeable `[~]` movement/drag → P7; 5.6d barrel re-export → P7.) **5.6g next** (palette app classes in `applications/sheets/palette/` — resolve `paletteClass`/`paletteCreateData` FIXMEs), then 5.7. |
| **5.7**  | **interaction/ (9) + animation/ (3) + top-level (board, loader, scene-manager, texture-extractor, framebuffer-snapshot) + extensions/ (4) + workers** — add `animation/{particle-generator,shake}`, `borders`, `transition`, `ktx2-parser`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [ ]                                                                                                                                                                                                                                                                                                                                              |

**Inherited deferrals this phase OWNS** (full annotations in the [Cross-phase deferrals](#cross-phase-deferrals-live) table):

- **Wall EDGE rename (3→5)** [Batch 5.1]: **DONE** (commit `10677cad7`) — EDGE\_\* consts added + WALL\_\* deprecated; all references flipped; `WallDocument` edge/darkness/initializeEdge added.
- **`shape`/`_shape` derived props (4→5/7)** [Batch 5.6]: author `client/data/shapes.d.mts` (`Rectangle`/`Circle`/`Cone`/`Ellipse`/`Polygon` `ShapeData`), then add the deferred typed `shape` on `tile`/`ambient-light`/`ambient-sound` + `_shape` on `drawing` (inline `// TODO(v14)` in each).

### Batch 5.1 — geometry/ (DONE — commit `10677cad7`)

Verified `[x]` (member-diffed, CI-green, type-tested): edge (rewrite: +darkness, level-keyed intersections, level params, EdgeTypes=wall|source|innerBounds|outerBounds, Edge.ThresholdData) · vertex (EDGE_SENSE/RESTRICTION, drop phantom isTerminal) · collision · observable-transform · unbound-transform · ray · limited-angle-polygon · polygon-mesher · weiler-atherton-clipping. Rename `[x]` quad-tree→quadtree. Add `[x]` shapes/elevated-surface-exposure-polygon (PolygonTree return FIXME `object|null`). Wall EDGE deferral `[x]`.

**`[~]` Level-entangled — finish in Phase 7 (Scene Levels):**

- **edges (CanvasEdges)** — `clear()/inititalize()/refresh()/getEdges` done; constructor `level` param + `get level()` FIXME-typed `object` until the `Level` document exists.
- **clockwise-sweep** — non-Level surface already v14 (`useInnerBounds`, `_determineEdgeTypes` param fixed to `PolygonType`, edgeTypes keyed by v14 EdgeTypes); still needs `Config.level` + the `this.level.id` intersection internals.
- **source-polygon (PointSourcePolygon)** — still missing `get level()`, `get scene()`, `surfaceExposure` (→PolygonTree), and `Config.level`. Not yet touched this batch.
- **`client/data/polygon-tree.d.mts`** — missing file (return type of `elevated-surface-exposure` + edge-tree); author in Phase 7 client/data, then drop the `object|null` FIXMEs.

> **Reminder — the 4 former-Phase-4 giants are NOT in this phase.** active-effect / region / scene / token are reassigned to **Phase 7** and blocked until their prerequisites exist — see the "client/documents Tier-B giant member-diffs" row in the [Cross-phase deferrals](#cross-phase-deferrals-live) table (full per-giant annotations in the archived Phase 4 detail). Don't get pulled back into them.

### Batch 5.2 — rendering/ (DONE — CI green)

**SCOPE FINDING (2026-05-26): v14 refactored the shader/filter base API.** Coupled ~60 files; the base change + all subclass renames landed in one atomic commit (CI cannot be green with a half-done base). 68 src + 37 test files; all six gates green (tsgo default, tsc --eOPT false, raw tsgo, test-types 463✓, eslint, prettier).

- **`BaseShaderMixin`** (`mixins/base-shader-mixin`) gained: `defaultUniforms` (source: getter → **modeled as a property** to avoid accessor/property-override churn across ~36 subclasses), `_createVertexShader(options?)`, `_createFragmentShader(options?)`, `_configure(options?)`, `_fragmentShaderCompatibility(options?)`, `_vertexShaderCompatibility(options?)` — the last two are `protected static` shims (`{since:14, until:16}`). (`create` stays on the concrete bases, matching the repo's existing simplification — source has an abstract no-op `create` on the mixin.)
- **`AbstractBaseShader`/`AbstractBaseFilter`** lost `static vertexShader`/`fragmentShader`/`defaultUniforms` (moved to mixin / replaced by the `_create*` methods); `create` gained a 2nd `options?` param.
- **Subclass recipe** (the dominant per-file change): `static [override] fragmentShader: X` → `protected static override _createFragmentShader(): string`; `static [override] vertexShader: string` → `protected static override _createVertexShader(): string`; `defaultUniforms` stays a property. `_create*` are **protected**, so `.test-d.ts` access them via bracket notation (`Shader["_createFragmentShader"]()`).

**Reusable findings (for 5.3+ and other shader-adjacent work):**

- **Holdout:** `shaders/samplers/amplification` keeps a real `static fragmentShader()` **method** (NOT a property; modeled `@deprecated since v14, until v16` since the base `_fragmentShaderCompatibility` shim routes it). The only file that did not rename to `_createFragmentShader`.
- **Variance hack:** `filters/glow-overlay` source overrides the generic `_createFragmentShader(options)` with positional `(quality, distance)` numbers; modeled as `_createFragmentShader(quality?: AnyObject | number, distance?: number)` (the `AnyObject` keeps it structurally assignable to the base) with an explaining `@remarks`. Genuine upstream signature inconsistency.
- **Deprecation removals done here (so Phase 8 needn't re-find them):** `AdaptiveLightingShader#getDarknessPenalty`, `AbstractBaseShader#_defaults`, `GlowOverlayFilter#animate` (all `@deprecated … until v14`). Also removed v14-gone non-deprecated members: weather `createProgram` + the weather/visibility/outline `create` overrides that no longer exist in source.
- **Phase 8 leftover:** `AbstractBaseShader.FragmentShaderFunction` namespace type is now orphaned (no referent after `fragmentShader` removal) — kept (removing an exported type is a deliberate breaking change); drop in Phase 8 cleanup.
- **Source getters the repo had as fields, now fixed:** `SHADER_HEADER` (coloration/illumination/background/darkness-lighting), `FRAGMENT_UNIFORMS` (base-lighting), `FRAGMENT_HEADER`/`commonUniforms` (base-weather).

**Sub-batch checklist (all DONE & CI-green):**

- **5.2a foundation** `[x]` — base-shader-mixin, fragment-channel-mixin, base-shader, \_types, blend-modes, batching/{batch-renderer (fixed `PreRenderBatchFunction` instance param),batch-shader-generator}, filters/{base-filter,base-mask-filter}.
- **5.2b lighting** `[x]` — base-lighting + coloration/illumination/background/darkness + 26 lighting/effects.
- **5.2c vision + weather** `[x]` — vision/\* (6) + weather/\* (5).
- **5.2d samplers + region + grid + graphics** `[x]` — samplers/\* (10), region/\* (3), grid/grid, graphics/dash-line; `_types` BatchData namespaces verified vs source.
- **5.2e filters** `[x]` — filters/\* (incl smaa/\*; `smaa/weights` class renamed `SMAABWeightCalculationFilter`→`SMAABlendingWeightCalculationFilter`).

### Batch 5.3 — sources/ + perception/ (DONE — CI green)

Pure member-diff (no shader-API coupling). 17 files + 1 new (`sources/_types.d.mts`). All six gates green.

**Sub-batch checklist (all DONE & CI-green):**

- **5.3a sources foundation** `[x]` — base-effect-source (added `get level()` + `SourceData.level`/`defaultData.level`), rendered-effect-source (already v14-accurate, no change), base-light-source (**removed `isDarkness`** — `@deprecated until v14`), point-effect-source (added `_getEdgeCreationOptions(): edges.Edge.ConstructorOptions`; fixed `priority`/`origin` order).
- **5.3b sources leaves** `[x]` — point-light (+`_getEdgeCreationOptions`, `testPoint`, `_canDetectObject(target, level)`; **removed `isDarkness`**), point-darkness (+`_getEdgeCreationOptions`; **removed `isDarkness`**), point-vision/point-sound/point-movement/global-light (already accurate). New `sources/_types.d.mts` passthrough + `_module` re-export.
- **5.3c perception** `[x]` — detection-mode (added static `_testCollision` + `_canDetect`'s 3rd `level` param), fog (removed `COMMIT_THRESHOLD`; added `sharedExploration`/`_createExplorationDocument`/`_unionizeSharedExploration`/`_applySharedExploration`/`_createExplorationRenderTexture`/`_getBase64ExtractionConfiguration`; `load`/`save` gained options; `fogExploration` now `boolean`), perception-manager (removed `until v14` flags + `refresh()`; added `refreshOccludedSurfaces`; `refreshEdges`→deprecated until v16), vision-mode (removed `until v14` `ShaderField` re-export), 5 detection-modes + `_types` (no change).

**Deprecation removals done here** (Phase 8 needn't re-find): `BaseLightSource#isDarkness` + `PointLightSource`/`PointDarknessSource` `isDarkness` overrides; perception-manager `refreshTiles`/`identifyInteriorWalls` flags + `refresh()`; vision-mode `ShaderField` re-export (all `@deprecated … until v14`).

**Scene-Levels FIXMEs introduced (→ P7, folded into the Scene Levels deferral):** `BaseEffectSource#get level()` (FIXME `object`); `PointLightSource#_canDetectObject(target, level)` + `DetectionMode#_canDetect(..., level)` `level` params (FIXME `object`); `DetectionMode._testCollision` relies on `test.level.isView`. `BaseEffectSourceData.level` is just a string id (typed `string | null`, not blocked).

### Batch 5.4 — containers/ + primary/ (DONE — CI green)

Pure member-diff (no shader-API coupling, no Level entanglement). 1 new file + ~15 changed + cascade disables. All six gates green.

- **New file** `primary/primary-canvas-particle-container.d.mts` (extends `CanvasTransformMixin(PIXI.Container)`: `elevation`/`sort` get-set, `shouldRenderDepth`, `renderDepthData`). Added to `primary/_module.d.mts` **first** (matching source order), and `PrimaryParticleEffect` moved to the `// Deprecated Exports` section there.
- **containers verified** `[x]` — `control-icon` got a full v13→v14 rewrite (now `RenderFlagsMixin(PIXI.Container)` base: `RENDER_FLAGS`/`renderFlags`/`applyRenderFlags`/`draw`/`_draw`/`_clear`/`_refresh`/`destroy`; `texture`/`size`/`elevation` getters; kept `iconSrc`/`rect`/`borderColor`/`tintColor`/`refresh(options)` as `until v16`). `point-source-mesh` (dropped 5 `until v14` shims), `sprite-mesh` (added culling/pass getters + `height` + `_renderWithCulling`), `cursor` (dropped dead `_animate`). `particles/{particle-effect,leaves}` are now wholly `@deprecated since v14, until v16`. The other containers (cached-container, full-canvas-mixin, unbound-container, door-control, door-mesh, resize-handle, precise-text, grid-highlight, grid-mesh, quad-mesh) were already accurate.
- **primary verified** `[x]` — `primary-canvas-object` (dropped `until v14` `document`/`updateBounds`), `primary-occludable-object` (rename `updateCanvasTransform`→`updateTransform`, `testOcclusion` now single-arg, `+_occlusionElevation`/`_occludedBySameElevationSurfaces`, `OcclusionState.surface`; dropped `until v14` `roof`/`containsPixel`), `primary-sprite-mesh` (`+_depthElevation`, batch-data fields; dropped 3 `until v14` shims), `primary-particle-effect` wholly `@deprecated since v14, until v16`. `primary-graphics`/`primary-canvas-container` already accurate.
- **Forced cross-batch edits** (prerequisites, like a coupling): `board.d.mts` (5.7) — added `INTERFACE` to `Canvas.PendingRenderFlags` (required by ControlIcon's `RENDER_FLAG_PRIORITY="INTERFACE"`; confirmed against the v14 board runtime, whose JSDoc typedef is stale). The 3 new `@deprecated` classes cascaded `// eslint-disable @typescript-eslint/no-deprecated` directives into their legitimate v14 consumers: `weather-effects.d.mts` (5.5), `tile.d.mts` (5.6), `client.d.mts` global aliases, and the leaves/particle-effect/weather-effects tests.
- **Deprecation removals done here** (Phase 8 needn't re-find): the `until v14` members above (point-source-mesh shims, cursor `_animate`, primary-canvas-object `document`/`updateBounds`, primary-occludable-object `roof`/`containsPixel`, primary-sprite-mesh `getPixelAlpha`/`_getAlphaBounds`/`_getTextureCoordinate`).
- **Carry to 5.6 (placeables/Tile):** `tile.d.mts`'s legacy `testOcclusion` shim still references the now-`@deprecated` `PrimaryOccludableObjectMixin.TestOcclusionOptions` (kept + disabled); clean up when Tile is verified.

### Batch 5.5a — groups/ + layers/base/ (DONE — CI green)

13 files (10 groups + 3 layer base classes). All six gates green (tsgo default, raw tsgo, tsc --eOPT false, eslint exit 0, prettier, test-types 463✓). The leaf layers (5.5b) and the 5 ShapeLayerMixin layers (→5.6) are **not** in this sub-batch — see the scope discovery in the roadmap row.

**groups/ verified `[x]`:**

- **canvas-group-mixin** — removed the deprecated global `BaseCanvasMixin` (gone from v14 client source). (`CanvasGroupMixin.Mixed` orphan-alias kept → Phase 8.)
- **rendered · overlay · hidden · interface · \_module** — already v14-accurate (member-diffed clean; no changes).
- **environment** — removed `get darknessPenalty()` + the deprecated `Config.darknessLevel` init-param.
- **effects** — removed the three `until v14` deprecated members (`get visibility`, `get globalLightSource`, `updateGlobalLightSource`); kept `initializeDarknessSources` (until v15); fixed the `TestInsideDarknessOptions extends _TestInsideLightOptions` namespace bug → `_TestInsideDarknessOptions`.
- **primary** — removed `mapElevationToDepth` (until v14); added `levelTextures: PrimarySpriteMesh[]` (TODO(v14-levels)); converted `hoverFadeElevation` field → `@deprecated since v14` get/set pair (TODO(v14-levels)).
- **visibility** — added `surfaceExposure: PIXI.Container | undefined` (TODO(v14-levels)); widened `testVisibility(points: …| …[])` + `_createVisibilityTestConfig(points: …[])` to arrays; added `level` (FIXME: Level → P7) to both `TestConfig` and `Test`.

**layers/base/ verified `[x]`:**

- **canvas-layer** — added `getZIndex(): number` (moved down from InteractionLayer where v13 had it) + `zIndex` to `LayerOptions`.
- **interaction-layer** — **now `declare abstract class`** (v14 InteractionLayer has no `_draw`; base `CanvasLayer._draw` is abstract in-repo, so removing the override requires abstract — ControlsLayer/PlaceablesLayer, its only concrete children, both define `_draw`). Added `static TOGGLE_PALETTE: Partial<SceneControls.Tool>`, `static prepareSceneControls(): SceneControls.Control | null`, `_highlightObjects`, `_onClickRight2`, `_onCycleViewKey`, and the keypress family (`_onSelectAllKey/_onDismissKey/_onUndoKey/_onCutKey/_onCopyKey/_onPasteKey`, all `(KeyboardEvent): boolean`). Changed `_onDeleteKey` return `Promise<void>`→`boolean`. Removed the non-source `_draw`/`getZIndex`.
- **placeables-layer** — v14 rewrite (1376-line source). Replaced `_copy`→`clipboard`; added `_configPreview`, `hasPreview`, `paletteCreateData` (FIXME: PaletteApplication → 5.6), `_throttleRotateMany`, `viewedDocuments`, `_prepareKeyboard{Movement,Rotation}Updates`, `setAllRenderFlags`, `_getCopyableObjects`, `_onUndo{Create,Update,Delete}`, `_storeHistory`, `_isCreationToolActive`, `_createDragPreviewData`, `_commitDragLeftDrop`, `_confirmDeleteKey`, and the `_onSelectAll/Dismiss/Undo/Cut/Copy/PasteKey` overrides. `copyObjects`→`(options?): ReadonlyArray`, `storeHistory`+`options`, `_sendToBackOrBringToFront`→`boolean|void`, `_onDeleteKey`→`boolean`, `moveMany` gained `dz`, `LayerOptions` gained `keyboardMovableObjects`/`confirmBeforeCreation`/`controlObjectAfterCreation`, `HistoryEntry` gained `options`. Removed `SORT_ORDER`, `_copy`, `_pasteObject` (v13), `gridPrecision` (until v14), `_highlight` get/set (until v13), and the non-source `_onDragLeftMove` override; `CREATION_STATES`(static) + `getDocuments` → `@deprecated since v14`.

**Forced cross-batch edit (prerequisite, like a coupling):** `tokens.d.mts` — removed its `override get gridPrecision()` (it overrode the now-removed base member; was `@deprecated until v14` anyway) + its test assertion. `drawings.d.mts`'s `gridPrecision` is a non-`override` own member (left for 5.6).

**Scene-Levels / palette FIXMEs introduced (→ deferrals):** placeables-layer `viewedDocuments`/`getDocuments`/`deleteAll` (Levels) + `paletteCreateData` (palette → 5.6); primary `levelTextures`/`hoverFadeElevation`; visibility `surfaceExposure`/`TestConfig.level`/`Test.level`. All folded into the live Scene-Levels deferral (P7) / the 5.6 palette work.

### Batch 5.5b — layers leaf (non-ShapeLayerMixin) + effects/ + masks/ + barrels (DONE — CI green)

26 files (14 src + 12 test). All six gates green (tsgo default, raw tsgo, tsc --eOPT false, eslint exit 0, prettier, test-types 463✓). The 5 `ShapeLayerMixin` leaf layers (drawings/lighting/sounds/regions/tiles) are **not** here — they're 5.6.

**leaf layers verified `[x]`:**

- **grid** (CanvasLayer) — `_drawMesh()`→`Promise<GridMesh>`; reordered `instance` after `layerOptions`; **removed 11 `@deprecated since v12, until v14` members** gone from v14 (`type`/`size`/`grid`/`isNeighbor`/`w`/`h`/`isHex`/`getTopLeft`/`getCenter`/`getSnappedPosition`/`measureDistance`).
- **controls** (**base-class change InteractionLayer→CanvasLayer** — v14 wires interaction via `canvas.registerMouseMoveHandler`, not activate/deactivate). Moved Hooks reg `InteractionLayerConfig`→`CanvasLayerConfig` (drops activate/deactivate hooks; `options.baseClass` now `typeof CanvasLayer`); `cursors`→`UnboundContainer`, `rulers`→`_rulerPaths`, +`pings`, +`getCursorForUser`; `drawRulers`/`updateRuler`→async, +`drawRuler`; `_onMouseMove(currentPos)`; `_deactivate` now non-override own method.
- **notes** — +`_getCopyableObjects`/`_tearDown`/`prepareSceneControls`; `_onClickLeft`→`Promise<void>`; removed `hintMapNotes` (gone in v14); layerOptions +`controllableObjects`/`confirmBeforeCreation`. `paletteClass`→FIXME 5.6.
- **templates** (`@deprecated since v14` in source — **class annotation deferred to P7** with the MeasuredTemplate reconciliation; only member surface done here). +`placeables`/`_getCopyableObjects`/`activate`/`prepareSceneControls`/`_createDragPreviewData`; removed non-source `_deactivate`/`_onDragLeftStart`.
- **tokens** (47 KB source) — large v14 movement-subsystem expansion. Added `_rulerPaths`/`_draggedToken`/`_dragMovementAction`/`turnMarkers`/`setTargets`/`recalculatePlannedMovementPaths`/`_updatePlannedMovements`/`_prepareKeyboardMovementUpdates`/`_getCopyableObjects`/`_onCycleViewKey`/`_confirmDeleteKey`/`prepareSceneControls`/`_highlightObjects`/`placeTokens`/`_onDropActiveEffect`/`_onClickLeft2`/`_onClickRight`/`_onClickRight2`/`_onDragLeftCancel`/`_onDismissKey`/`_cancelPlacement`/`_cancelMovementPlanning`; `storeHistory`+3rd `options`; layerOptions +`keyboardMovableObjects`/`confirmDeleteKey`; removed `toggleCombat` (gone in v14). `_movementPlanningContext`/`_placementContext` (→`AnyObject|null`), `_updatePlannedMovements` values, `placeTokens` options FIXME'd → P7 (token-movement). `paletteClass`→FIXME 5.6.
- **walls** (22 KB source) — finished from 5.1's EDGE pass. `last`→`_last`; removed `_cloneType`/`_panCanvasEdge`/`_getWallDataFromActiveTool`(+`WallTools`)/`_onDragLeftStart`/`_onClickRight` (not in v14) + 5 dead deprecated (`checkCollision`/`highlightControlledSegments`/`initialize`/`identifyInteriorWalls`/`identifyWallIntersections`). Added `prepareSceneControls`/`_createDragPreviewData`/`_onUndoCreate`; layerOptions +`controlObjectAfterCreation`. `paletteClass`→FIXME 5.6.

**effects/ verified `[x]`:** background (fixed `TearDownOptions extends DrawOptions` typo) · coloration (clean) · darkness (+`filter: VoidFilter | undefined`) · illumination (removed v12/v13 `backgroundColorTexture`/`backgroundColor`/`_createBackgroundColorTexture`/`render`/`updateGlobalLight`/`background`/`globalLight`) · weather (+`hookName`; `effects` Map values single not array; removed v11 `weather`; `ParticleGenerator`→FIXME 5.7).

**masks/ verified `[x]`:** depth (+`inclusive` param on `mapElevation`) · occlusion (v14 rewrite: RGB→RGBA, +`surfaces`/`occluded`/`occludedSurfaces`, `_updateOcclusionStates`→`_updateOccludedObjects`, +`_updateOccludableTokens`/`_updateOccludedSurfaces`, `updateOcclusion`→`@deprecated since v14`, removed `_identifyOccludedTiles`; `occludedSurfaces` element type → FIXME `RegionSurface` P7) · vision (**caution: `_types.mjs` typedef is incomplete** — verified against `CanvasVisibility#createVision` runtime: kept `containmentFilter`/`light.global`/`light.sources`, added v14 `surfaceExposure`+`shared` on light/mask/sight; removed the 6 `until v14` deprecated getters `base`/`fov`/`los`/`lights`/`lightsSprite`/`tokens`).

**barrels `[x]`:** `_module` matched source (no change). `_types` unchanged + a `// TODO(v14, 5.6)` note for v14's new `RegionPlacementOptions` typedef (rides with `RegionLayer`).

**Deprecation removals done here** (Phase 8 needn't re-find): grid's 11 v12-deprecations; illumination's `updateGlobalLight`/`background`/`globalLight`; weather's `weather`; tokens' `toggleCombat`; walls' `checkCollision`/`highlightControlledSegments`/`initialize`/`identifyInteriorWalls`/`identifyWallIntersections`; the 6 vision-container deprecated getters; occlusion's `_identifyOccludedTiles` (v11→v13).

### Batch 5.6a — placeable-object.d.mts (DONE — CI green)

The `PlaceableObject` base — foundation for all 22 placeables. Member-diffed against `placeables/placeable-object.mjs` (1622 lines). All six gates green (tsgo default, raw tsgo, tsc --eOPT false, eslint, prettier, test-types 463✓).

**`[x]` placeable-object** — added v14 members in source order: `static get implementation()` (→ `PlaceableObject.AnyConstructor`, matching `PlaceablesLayer.placeableClass`); `get isVisible()` / `get isInteractable()`; `get previewType()` + the `_previewType` field (set in ctor); `static _getCopiedObjectsOrigin()`; `protected _refreshVisibility()` / `_refreshState()` / `_clear()`; the `static _getShiftedPosition(dx, dy, dz, position, snapped, grid)` overload; `_initializeDragLeft` / `_finalizeDragLeft` / `_initializeDragRight` / `_finalizeDragRight`. Signature changes: instance `_getShiftedPosition` gained `dz` + now returns `Canvas.ElevatedPoint`; `_canDragLeftStart` gained 3rd `options?:{notify?}`; `_onHoverIn` options gained `updateLegend`; `_onHoverOut` gained an `options?:{updateLegend?}` param; `_pasteObject` options gained `cut`. `cullable` `@defaultValue` fixed `false`→`true`. New namespace types: `PreviewType`, `HoverOutOptions`, `CanDragLeftStartOptions`.

**`RENDER_FLAGS` cascade (key finding).** v14 base `RENDER_FLAGS` adds `refreshVisibility` and `refreshState` now propagates `["refreshVisibility"]`. Because each placeable's `renderFlags: RenderFlags<X.RENDER_FLAGS>` must stay assignable to the base, **every subclass `RENDER_FLAGS` must contain `refreshVisibility`** (variance). `light`/`note` already inherit it (their interfaces `extends PlaceableObject.RENDER_FLAGS`); `token` already had it; the 6 standalone ones (`drawing`/`region`/`sound`/`template`/`tile`/`wall`) got the key **force-added as a prerequisite** (v14-accurate — all 9 subclasses have it in source) — their **full** RENDER_FLAGS member-diff (order, other propagates, missing keys) remains their own sub-batch. All 7 affected `.redraw.propagate` test unions updated.

**`clear()` deprecation (forced cross-batch prerequisite).** v14 base `clear()` is `@deprecated since v14` (no-op, returns `this`); subclasses replaced their `clear()` with protected `_clear()`. Removed the stale `override clear()` from `tile`/`token` (`:void`) + `drawing`/`region` (`:this`, the "fake override" ones); they now inherit the deprecated base `clear()` + base `protected _clear(): void`. Token's `_getShiftedPosition` override fixed to `(dx, dy, dz): Canvas.ElevatedPoint` (was `(number, number): Canvas.Point`). Tests calling `.clear()` got `// eslint-disable-next-line @typescript-eslint/no-deprecated`.

**Carry to 5.6b/c/e:** the 6 standalone subclass `RENDER_FLAGS` need their full v14 member-diff (only `refreshVisibility` was touched here). `_clear()` may want a doc-only override per subclass when each is verified (the runtime bodies differ; the `protected _clear(): void` signature is inherited correctly for now).

### Batch 5.6b — non-shape leaf placeables (DONE — CI green)

5 src + 4 test files: `note`, `wall`, `template` (the MeasuredTemplate placeable), `regions/geometry`, `regions/mesh`. All six gates green (tsgo default, raw tsgo, tsc --eOPT false, eslint, prettier, test-types 460✓). (`region`/`drawing`/`light`/`sound`/`tile` are shape-leaf → 5.6e; `regions/shape-controls` → 5.6d.)

- **`[x]` note** — v14 RENDER_FLAGS rework: `refresh` now propagates `["refreshState", "refreshTransform", "refreshTooltip", "refreshElevation"]`; **added `refreshTransform`** (alias → `["refreshPosition", "refreshSize"]`) **+ `refreshSize`**; **removed deprecated `refreshText`** (was `until v14`). Added `get isAuthor`, `override get isInteractable`, `override _overlapsSelection`, `_refreshSize`; marked `isVisible` `override`; `_drawTooltip` return `PIXI.Text`→`PreciseText`. Removed the `clear()` fake override (inherits deprecated base `clear()`), the pointless `_canConfigure` narrow (inherits base), and the two `until v14` deprecated getters `text`/`size`.
- **`[x]` wall** — RENDER_FLAGS already v14-correct (5.6a). Added `get doorMeshes(): Set<DoorMesh>`, `override _pasteObject` (typed with the standard `PasteObjectReturn` like token — runtime returns `{c, levels}` but the type stays the base return; **no Scene-Levels coupling needed**), `get hasDoorMesh`/`createDoorMeshes`/`destroyDoorMeshes`; `_onHoverOut` gained the `options?` param. Replaced the `clear()` fake override with `protected override _clear(): void`. **Removed the stale `_refreshState`** (not in v14 source — inherited). `initializeEdge` → `@deprecated since v14` and moved to the deprecations section. **Removed 8 `until v14` deprecated members**: `roof`, `hasActiveRoof`, `identifyInteriorState`, `orientPoint`, `applyThreshold`, `vertices`, `A`, `B`. Reordered the class body to match source (createDoorControl/clearDoorControl moved down into the Interactivity section).
- **`[x]` template (MeasuredTemplate placeable)** — RENDER_FLAGS already v14 (5.6a). Added `_refreshVisibility` + `testPoint`. Removed the stale `isVisible` override (inherited), the `clear()` fake override, and the stale `_canControl` override (not in v14 — inherited). Marked the 4 static shape helpers (`getCircleShape`/`getConeShape`/`getRectShape`/`getRayShape`) `@deprecated since v14, until v16`; corrected `borderColor`/`fillColor`/`owner` from `until v14` → **`until v16`** (the v14 warnings are `{since:12, until:16}`, so they persist). **The whole class is `@deprecated since v14` in source — the class-level annotation is DEFERRED to P7** with the MeasuredTemplate reconciliation (cascades into `src/configuration/`, the layer, and globals); only the member surface is done here (a `@privateRemarks` note marks it in-file).
- **`[x]` regions/geometry** — already v14-accurate, no change.
- **`[x]` regions/mesh** — added the missing `get geometry()`/`set geometry()` (`RegionGeometry | PIXI.Geometry`); `containsPoint` param `PIXI.IPointData`→`Canvas.Point` (v14 source uses `{Point}`; dropped the stale privateRemark).

**Deprecation removals done here** (Phase 8 needn't re-find): wall's `roof`/`hasActiveRoof`/`identifyInteriorState`/`orientPoint`/`applyThreshold`/`vertices`/`A`/`B`; note's `text`/`size` (all `until v14`).

### Batch 5.6c — tokens/ subdir + token placeable

Split into **part A (tokens/ subdir — DONE & CI-green, commit `d40f79738`)** and **part B (the 5861-line `placeables/token.mjs` — `[~]` non-movement surface DONE & CI-green, commit `666e0f6cc`; movement/drag → P7)**.

**Part A `[x]` — placeables/tokens/ (8 files + 5 tests; all six gates green, test-types 463):**

- **ring-data** — add v14 effect `COLOR_OVER_SUBJECT` to the `effects` `ObjectField` (source has 5 entries, repo had 4); add the source's real `_validateType` override (kept the existing `_cast` because the repo models `DataField._cast` as **abstract**, so a concrete field must declare it — a type-system necessity, noted in-file).
- **ring-config** — drop the v14-removed `configNames` deprecated getter (`until v14`). Member surface otherwise matched.
- **ring** — add v14 `maskName`/`maskUVs` instance fields + `maskName` on `RingData`; add the `COLOR_OVER_SUBJECT` effect to `Effects`; add `static easePingPong`; widen `configure(mesh?: …| null)` + `configureSize(options?)` (new `ConfigureSizeOptions` interface, `fit: CONST.TEXTURE_DATA_FIT_MODES`); **drop the v14-gone `configureMesh`/`configureNames`** (`@deprecated since v12` empty shims).
- **turn-marker-data** — filled stub: `TurnMarkerData` DataModel `Schema` (id, label, config:{shader: `clientFields.ShaderField`, spin, pulse:{speed,min,max}}) + the `TurnMarkerAnimationData`/`TurnMarkerAnimationConfigData` namespace typedefs (defined in this `.mjs`, **not** in `placeables/_types.mjs` — the turn-marker.mjs `@import "../_types.mjs"` is stale).
- **turn-marker** — filled stub: `TokenTurnMarker extends PIXI.Container` (token, mesh:`SpriteMesh`, animation, draw, animate).
- **base-ruler** — typed `refresh(rulerData: DeepReadonly<TokenRuler.Data>)` (was `unknown` — `TokenRuler.Data` resolves now). Otherwise already v14-accurate.
- **ruler** — already well-developed (full `TokenRuler.Data`/`.Waypoint` namespace); added the 3 missing protected methods `_preparePath`/`_shouldRenderWaypoint`/`_accumulateWaypointData` (between `destroy` and `refresh`). **No new movement FIXMEs needed** — `TokenRuler.Data`/`.Waypoint` resolve via the `Token.MeasuredMovementWaypoint`/`.PlannedMovement` stubs (currently `unknown` in token.d.mts — the P7 token-movement deferral).
- **targets** + **\_module** — already v14-accurate, no change.

**Deprecation removals done here** (Phase 8 needn't re-find): ring-config `configNames`; ring `configureMesh`/`configureNames`.

**Part B `[~]` — the token placeable** (`placeables/token.d.mts`): **non-movement v14 surface DONE & CI-green** (commit `666e0f6cc`). The token-movement/drag subsystem stays deferred to **P7** (rides with the Tier-B `token` giant + the movement `_types`), so the file is **`[~]` partial**.

- **Done (non-movement):**
  - Fields (source order): added `effects`, `levelIndicator`, `targetArrows`, `targetPips`, `ruler` (`BaseTokenRuler | null | undefined`), `_plannedMovement` (`Record<string, Token.PlannedMovement>` — movement stub), `_visionSourceVersion`, `turnMarker` (`TokenTurnMarker | null`); **removed the `target` field** (→ deprecated getter).
  - `RENDER_FLAGS`: added `refreshRuler` + `refreshTurnMarker`; fixed `refresh`/`refreshMesh` propagate comments.
  - Getters: `movementAnimationName`, `movementAnimationPromise`, `showRuler`, `_preventKeyboardMovement`, `isDragged`, `isInteractable` (+ moved `animationName` up to source order).
  - Methods: `_initializeRuler`, `_createSharedFogVisionSource`, `_isFogExplorationSource`, `_refreshTurnMarker`, `_refreshRuler`; `_drawTarget`→`_drawTargetArrows` + new `_drawTargetPips`; `_refreshTarget()` now no-arg; `_onHoverOut` gained `options` (5.6a base change).
  - Deprecations: **removed** the v13-gone (`updatePosition`, `refreshHUD`) + the `until v14` shims (`updateSource`, `getCenter`, `owner`, `toggleCombat`, `toggleEffect`, `toggleVisibility`, `_recoverFromPreview`); **relocated to deprecations** `testInsideRegion`/`segmentizeRegionMovement`/`getSize` (`since v13, until v15`) + added `get target` + moved `getMovementAdjustedPoint` (`since v14, until v16`).
- **Deferred to P7 (token-movement/drag subsystem — folded into the Tier-B `token` deferral):** `planMovement`, the constrain/find/`createTerrainMovementPath` internals, `recalculatePlannedMovementPath`, the `_getDrag*`/`_changeDragElevation`/`_addDragWaypoint`/`_removeDragWaypoint`/`_getDragWaypointPosition`/`_getDragOrigin`/`_updateDragDestination`/`_triggerDragLeft*`/`_onDragClick*`/`_onDragMouseWheel`/`_initializeDragLeft`/`_finalizeDragLeft`/`_onDragLeftDrop`/`_shouldPreventDragLeftDrop`/`_getDragLeftDropUpdateOptions` family, `panCanvas`, `_getKeyboardMovementAction`, `_getHUDMovementPosition`/`_getHUDMovementAction`/`_getConfigMovementPosition`, the `_getAnimationMovementSpeed`/`_modifyAnimationMovementSpeed`/`_getAnimationRotationSpeed`/`_requiresRotationAnimation`/`_getAnimationTransition` animation helpers, `static _getDropActorPosition`, and the v13-leftover `_onDragLeftStart` (gone in v14). These need the movement `_types` (`Token.MeasuredMovementWaypoint`/`.PlannedMovement` etc., currently `unknown` stubs).

### Batch 5.6d — shapes foundation (DONE — CI green)

4 new src files + 4 new tests. All six gates green (tsgo default, tsc --eOPT false, raw tsgo, eslint, prettier, test-types 467✓).

- **`[x]` `client/data/shapes.d.mts`** — `ClientShapeDataMixin` (module-private inner `ClientShapeData` + `ClientShapeDataMixin<BaseClass>(ShapeData)` returning `Mixin<typeof ClientShapeData, BaseClass>`; `BaseClass = AnyConstructor`) + the 10 client subclasses each `extends ClientShapeDataMixin(Base<X>ShapeData)` (common base imported aliased `as Base<X>ShapeData` to dodge the same-name clash). Mixin surface: `scene`/`grid`/`gridlessGrid`/`isEmpty`/`polygons`/`polygonTree`/`clipperPaths`/`clipperPolyTree`/`triangulation`/`bounds`/`origin`/`center`/`area`/`measuredSegments`/`controlHandles`/`isAffectedByGrid`/`hasRotationalSymmetry` getters + `_toClipperPath`(static)/`_onShapeChange`/`_onGridChange`/`_createRay`/`_getSnappedPoint`/`_calculateSize`/`_getSnappedRotation`/`testPoint`/`_createClipperPolyTree`/`_createOrigin`/`_createCenter`/`_calculateArea`/`move`/`rotate`/`_rotate`/`drawShape`/`drawReferenceLines`/`_createMeasuredSegment`/`_createMeasuredSegments`/`_createControlHandles`/`moveControlHandle`/`_moveScaleHandle`/`_moveRotationHandle`/`_moveSweepHandle`/`_onDragStart`/`_onDragMove`/`sampleInterior`/`sampleBoundary`. Shared namespace types `MeasuredSegment`/`ControlHandle`/`ControlHandles`/`Triangulation`/`ClipperPolyTreeResult` + option bags. Each subclass redeclares its v14 overrides in source order with `override` (it extends the mixin result — real `extends`, so `override` is correct). **Decisions:** (1) `_updateCommit` omitted — not modeled on the repo's `DataModel`; (2) `clone`/`_onShapeChange` identical-signature cache overrides omitted in subclasses (zero type surface); (3) `get polygonTree()` (mixin + Token override) FIXME-typed `AnyObject` — awaiting the missing `client/data/polygon-tree.d.mts` (`PolygonTree`), P7; (4) `TokenShapeData._getTokenShape()` typed `Circle|Ellipse|Polygon|RectangleShapeData` (source JSDoc omits Rectangle but the runtime constructs it — `@privateRemarks`).
- **`[x]` `placeables/mixins/shapes.d.mts`** (new `mixins/` dir, no barrel — matches source) — `ShapeObjectMixin` over `PlaceableObject` (`BaseClass = PlaceableObject.AnyConstructor`). Inner `ShapeObject` class declares members WITHOUT `override` (mixin inner classes don't `extends` the base in the declaration — `Mixin` intersects, so there's no override-compat constraint; base-method overrides match the base param/return types — `Canvas.Event.Pointer`, etc. — and carry `@remarks`). `_prepareDragLeftDropUpdates` returns `PlaceableObject.AnyDragLeftDropUpdate[]` (the runtime returns shape/position updates; uses the base union per the documented unsound subclassing) — no edit to `placeable-object.d.mts`.
- **`[x]` `containers/elements/shape-controls.d.mts`** (def. from 5.4) — `ShapeControls<DocumentClass, ObjectClass, LayerClass, ShapeClass = BaseShapeData> extends RenderFlagsMixin(PIXI.Container)` (real `extends` → `override` on `applyRenderFlags`/`destroy`; `RENDER_FLAGS`/`renderFlags`/`RENDER_FLAG_PRIORTY="INTERFACE"` modeled like `ControlIcon`) + `ShapeControlsHandle extends PIXI.smooth.SmoothGraphics`. `handles` getter typed `PIXI.Container` (repo PIXI isn't generic over children — `@remarks`). Added `export * from "./elements/shape-controls.mjs"` to the containers barrel (source order: after `sprite-mesh`, before Deprecated).
- **`[x]` `placeables/regions/shape-controls.d.mts`** — `RegionShapeControls extends ShapeControls<RegionDocument.Implementation, Region.Implementation, RegionLayer>` (`override _updateDragPreview`/`_onClick2`). Added to the regions barrel.

**Deferral introduced (5.6d → P7):** the client-data barrel re-export (the shadow). See the new row in the live deferrals table.

**Carry to 5.6e:** the shape leaf placeables (drawing/light/sound/tile/region) consume the shape mixin and type `document.shape`/`document.shapes` as the client shape subclasses — import them directly from `#client/data/shapes.mjs` (not the barrel, which is deferred). The deferred `shape`/`_shape` derived-props on the canvas leaf docs can now be authored (the `ShapeData` classes exist). **But see the 5.6e cycle finding below — placeables CANNOT `extends ShapeObjectMixin(PlaceableObject)`.**

### Batch 5.6e — shape leaf placeables (DONE — CI green)

**All five shape leaf placeables (`drawing`/`light`/`sound`/`tile`/`region`) + the doc `shape`/`_shape` derived-props DONE & CI-green** (all six gates; test-types 468 files / 460✓).

**`light`/`sound`/`tile` done `[x]`** — all three were full v14 shape rewrites: `extends ShapePlaceableObject<Doc>` (the cycle workaround), `controls: ShapeControls.Any` + `tooltip: PreciseText` (light/sound), new `refreshTransform`/`refreshField`/`refreshTooltip`/`refreshMeasurements` RENDER_FLAGS, `refreshElevation`→`@deprecated since v14, until v16` alias (light/sound), `refreshFrame`→deprecated (tile). Added `isInteractable`/`_getMeasuredShapes`/`_onControl`/`_onRelease`/`_updateDragPreviews`/`_refreshTooltip`/`_getTooltipText`/`_getTextStyle`. **Removed** the v13 control-icon surface: `refreshControl`, `_refreshElevation` (method), the `clear()` fake override (inherit deprecated base), and the fake `_onHoverIn`/`_onDragLeftMove`/`_onDragEnd`/`_prepareDragLeftDropUpdates`/`_canDragLeftStart` overrides (now inherited from `ShapePlaceableObject`/base). Light/sound `updateSource`/`source` (`until v14`) **removed**. Tile fully rewritten: dropped `_overlapsSelection`/`_refreshFrame`/`activateListeners`/all `_onHandle*`/`_onDragLeft*` overrides + the `FrameContainer` interface (`frame` is now a plain `PIXI.Container`), `bg`→`PIXI.Sprite | null`, `texture`/`mesh`→drop `| undefined` (field-initialized `null`); `createPreview`→`@deprecated since v14, until v16`; **removed 5 dead deprecations** (`isRoof`/`testOcclusion`/`containsPixel`/`getPixelAlpha`/`_getAlphaBounds`, all `until v13/v14` — resolves the 5.4 Tile-`testOcclusion` carry note). **Finding:** sound's `_getSoundSourceData` runtime returns `preview`, but `PointSoundSource.SourceData` has no `preview` (sound sources aren't rendered-effect sources) — `SoundSourceData` adds only `level` (now in `BaseEffectSource.SourceData`), not `preview`.

**Doc `shape`/`_shape` derived-props done `[x]`** (resolves the 4→5.6e deferral): `AmbientLightDocument.shape: CircleShapeData | ConeShapeData`, `AmbientSoundDocument.shape: CircleShapeData`, `TileDocument.shape: RectangleShapeData`, `DrawingDocument._shape: RectangleShapeData | EllipseShapeData | PolygonShapeData` (internal). All imported from `#client/data/shapes.mjs` (the **client** subclasses — the common `foundry.data.*` bases would be wrong; the client-data barrel re-export is still deferred to P7). Tests import the client classes the same way.

**🔑 THE CYCLE FINDING — reuse this; 5.6f needs it too.** v14 source declares these placeables `extends ShapeObjectMixin(PlaceableObject)`. **fvtt-types/tsgo cannot model the mixin-CALL in an `extends` clause here:** a mixin call forces tsgo to evaluate the base **eagerly** while the subclass module is still loading, and that eager eval transitively resolves `Canvas` → the `foundry.canvas` namespace → the canvas barrel's `export * as placeables` → the placeables barrel, **which re-exports the very subclass being declared** → circular import alias (`TS2506`/`TS2303`). A control test (an identical mixin-extended class _not_ re-exported by any barrel) does **not** cycle — confirming the trigger is solely "subclass re-exported by a barrel its eager mixin-base reaches." A full de-barrel is infeasible (`board.d.mts`/the `Canvas` god-object references `foundry.canvas.*` pervasively). **The fix (decided with maintainer):** the mixin file `placeables/mixins/shapes.d.mts` now ALSO exports `declare abstract class ShapePlaceableObject<Doc> extends PlaceableObject<Doc>` (the mixin instance modeled as a concrete subclass; members mirror the kept-for-fidelity `ShapeObject` + `ShapeObjectMixin` function, which the 5.6d test still uses). **Shape placeables `extends ShapePlaceableObject<Doc>` — a LAZY direct-generic heritage (like every other placeable's `extends PlaceableObject<Doc>`), so no eager eval, no cycle.** **⚠ 5.6f's `ShapeLayerMixin(PlaceablesLayer)` will hit the identical problem → give it the same `ShapeLayerPlaceablesLayer<...>`-style intermediate.**

**Two tsc-bug workarounds (eslint-only; tsgo/tsc/test-types unaffected).** Type-aware ESLint runs the `tsc` API and hit `Debug Failure ... parameter should have errors when reporting errors` — a **spurious crash surfacing in an unrelated `DialogV2.input` overload** (`dialog.test-d.ts:133`), triggered by perturbation from the new types. Isolated to two constructs; both worked around (documented in-file):

1. **Don't reference a `ShapeControls` _subclass_ type from a placeable.** Typing `Drawing#controls` as `DrawingShapeControls` crashed; typing it as **`ShapeControls.Any`** (base) does not (and is more precise than the source's `@type {PIXI.Container}` JSDoc). Use `controls: ShapeControls.Any` for all shape placeables.
2. **Don't pass a client-shape `ShapeClass` arg to `ShapeControls`.** The 4 new shape-controls subclasses (`drawings`/`lights`/`sounds`/`tiles`) **omit** the `ShapeClass` type argument (default `BaseShapeData`, matching `RegionShapeControls`) — declaring `RectangleShapeData | …` (the generic-heavy `ClientShapeDataMixin` classes) as that arg crashed. Also: don't narrow `Drawing#_initializeDragShape`'s return to that client-shape union (omitted; inherits `ShapePlaceableObject`'s `BaseShapeData`).

**Done `[x]`:**

- **mixins/shapes.d.mts** — added `ShapePlaceableObject` (+ `export { ShapePlaceableObject }`); kept `ShapeObject`/`ShapeObjectMixin`/namespace untouched (5.6d test passes).
- **drawings/lights/sounds/tiles `shape-controls.d.mts` + `_module.d.mts`** (8 new files) — `XShapeControls extends ShapeControls<DocImpl, PlaceableImpl, Layer>` (ShapeClass omitted per workaround #2), source-ordered overrides; `_prepareDragDropUpdate` return typed `AnyObject`. Added `drawings`/`lights`/`sounds` namespace re-exports to `placeables/_module.d.mts` (source order; **no `tiles` namespace** — source omits it).
- **drawing.d.mts** — `extends ShapePlaceableObject<DrawingDocument.Implementation>`. Removed v13 members now inherited/gone (`isVisible`/`_canControl`/`_canConfigure`/`_onHoverIn`/`activateListeners` inherited; `_onMouseDraw`/`_addPoint`/`_removePoint`/`_onHandle*`(6) gone). Added `_clear`/`_refreshVisibility`/`controls: ShapeControls.Any`/`_updateDragPreviews` overrides. RENDER_FLAGS: `refreshRotation`→`{}`, `refreshSize` drops `refreshFrame`, **`refreshFrame`→`@deprecated since v14, until v16`**, **`refreshMesh` removed**. `texture`→`PIXI.Texture | null`. Dropped orphaned `AddPointOptions`.
- **lighting.d.mts** (prerequisite, 5.6f file) — **removed the v13-leftover `_onMouseWheel` override** (gone in v14 source; inherited base; it returned `Promise<AmbientLight>` which made `LightingLayer` fail `PlaceablesLayer.Any`, blocking `AmbientLightShapeControls`). Full v14 member-diff of lighting is still 5.6f.
- Tests updated: `drawing.test-d.ts` (removed gone-member assertions, `refreshMesh`; added `_clear`/`_refreshVisibility`/`controls`/`hoveredHandle`; `_onHoverIn`→`void | false`), `lighting.test-d.ts` (`_onMouseWheel` inherited return).

### Batch 5.6f — ShapeLayerMixin + 5 leaf layers (DONE — CI green)

1 new src file + mixin + 5 reparented leaf layers + 5 tests. All six gates green (tsgo, tsc --eOPT false, raw tsgo, eslint, prettier, test-types 468/460). Commit `b713b348c`.

- **`[x]` `layers/mixins/shapes.d.mts`** (new) — `ShapeLayerMixin` function (fidelity, inner `ShapeLayer` class, no barrel — matches source) **plus `ShapeLayerPlaceablesLayer<DocumentName> extends PlaceablesLayer<DocumentName>`**, the cycle-dodging intermediate (identical pattern to 5.6e's `ShapePlaceableObject`). Surface: `_mouseWheelContext`, `static layerOptions` (`ShapeLayerMixin.LayerOptions` adds `allowedEmptyShapes`/`discardClosingPoint`), `getSnappedPoint`, the drag/click family (`_onClickLeft`/`_onClickLeft2`/`_canDragLeftStart`/`_onDragLeftStart`/`_onDragLeftMove`/`_onDragLeftDrop`/`_commitDragLeftDrop`/`_onDragLeftCancel`), `_createDragShapeData`/`_updateDragPreview`, and the mouse-wheel family (`_onMouseWheel`/`_updateMouseWheelShape`/`_updateMouseWheelPreview`/`_cancelMouseWheel`/`_prepareMouseWheelUpdate`). **`_onMouseWheel` kept `Promise<ObjectFor[]|void>`** (assignable to the base; runtime is sync — privateRemark).
- **`[x]` drawings** — `extends ShapeLayerPlaceablesLayer<"Drawing">`; +`_getCopyableObjects`/`prepareSceneControls`/`_createDragPreviewData`/`_createDragShapeData`(literal union)/`_updateMouseWheelPreview`; `discardClosingPoint:false` layerOption. **Removed** `DEFAULT_CONFIG_SETTING`/`configureDefault` (v14-gone), `gridPrecision` (`until v14`), and the per-leaf `getSnappedPoint`/click/drag overrides (now inherited from the mixin).
- **`[x]` lighting** — `extends ShapeLayerPlaceablesLayer<"AmbientLight">`; **+`controllableObjects:true` layerOption** (v14); +`prepareSceneControls`/`_createDragShapeData`/`_updateDragPreview`/`_updateMouseWheelPreview`; dropped the per-leaf `_canDragLeftStart`/`_onDragLeft*` overrides + the 5.6e `_onMouseWheel`-removal note (now inherited).
- **`[x]` sounds** — `extends ShapeLayerPlaceablesLayer<"AmbientSound">`; **+`controllableObjects:true`/`confirmBeforeCreation:true`** layerOptions; +`prepareSceneControls`/`_createDragShapeData`/`_updateDragPreview`. `refresh`→`void` (was `number|void`); **`getListenerPositions`/`_syncPositions`/`AmbientSoundPlaybackConfig.listener` → `Canvas.ElevatedPoint`** (v14 `_configurePlayback` reads `.elevation`, no longer `.equals`); `previewSound` param `Point|ElevatedPoint`; `_onMouseMove` gains `currentPos`. Dropped the four per-leaf drag overrides.
- **`[x]` tiles** — `extends ShapeLayerPlaceablesLayer<"Tile">`; **+`confirmBeforeCreation:true`**; +`prepareSceneControls`/`_createDragPreviewData`/`_createDragShapeData`/`_updateDragPreview`/`_updateMouseWheelPreview`. **Removed** the `controllableObjects` override (v12-era) + 3 dead deprecations (`roofs` `until v14`, `textureDataMap`/`depthMask` `until v13`); dropped per-leaf `getSnappedPoint`/drag overrides.
- **`[x]` regions** — full v14 rewrite `extends ShapeLayerPlaceablesLayer<"Region">`. **+`templateMode` get/set, `_togglePaletteVisible`, `_highlights`, `_shapeClipboard` (`RegionLayer.ShapeClipboard`), `_placementContext` (`AnyObject|null`, per the 5.6c internal-context precedent), `placeRegion`/`placeRegions` (public API, typed with the new `RegionLayer.PlacementOptions`), `_cancelPlacement`, and the key handlers (`_onDeleteKey`/`_onCutKey`/`_onCopyKey`/`_onPasteKey`/`_onDismissKey`/`_confirmDeleteKey`)**. `copyObjects` now returns `ReadonlyArray<Region.Implementation>` (v14 copies; was hardcoded `[]`); `storeHistory` gains `options`; `_highlightShape` param `BaseShapeData|null` (was CreateData). **Removed** `legend`/`_holeMode` (v14-gone) + per-leaf click/drag overrides. **Resolves the `layers/_types` `RegionPlacementOptions` TODO** (`= RegionLayer.PlacementOptions`).

**`region` done `[x]`** — full v14 rewrite `extends ShapePlaceableObject<RegionDocument.Implementation>` (no `controls` member exposed → didn't hit the eslint tsc-crash; `#controls` is private). New v14 surface: `isInteractable`, `animationState` (new `Region.AnimationState` namespace interface, exposed `DeepReadonly`), `isAnimating`, `_redrawShapeControls`, `_getMeasuredShapes`, `_refreshVisibility`/`_refreshShapes`/`_refreshGeometry`, `_getCoveredGridSpaceOffsets(): foundry.grid.BaseGrid.Offset2D[]`, `_onTokenAnimationFrame`, `_onAnimationStateChange`, `_updateDragPreviews`, `_clear`/`_destroy`. RENDER_FLAGS rewritten (refreshShapes/refreshGeometry/refreshMeasurements replace refreshBorder-only). **The v13 members the prior repo had as live (`shapes`/`bottom`/`top`/`polygons`/`polygonTree`/`clipperPaths`/`triangulation`/`segmentizeMovement`/`testPoint` + statics `CLIPPER_SCALING_FACTOR`/`MOVEMENT_SEGMENT_TYPES`) are all `@deprecated since v13, until v15` in v14** — moved to a deprecations section + marked. Removed the `_canDrag` override (inherited). `_pasteObject` left off (override returns shape/level data assignable to the inherited `PasteObjectReturn` — **no Scene-Levels coupling**). **FIXME(P7):** `Region.AnimationState.polygonTree` typed `RegionPolygonTree` but the animated branch yields the unauthored `PolygonTree` (`#client/data/polygon-tree.mjs`). **Lint gotcha:** the deprecated static `MOVEMENT_SEGMENT_TYPES` collides by name with the brand **type** alias → a block `eslint-disable @typescript-eslint/no-deprecated` wraps the `MovementSegment`/brand/`MovementSegmentTypes` type cluster (type-position refs, not the deprecated static).

---

## Inherited deferrals (action these in Phase 5)

_Folded in from the old central deferrals table. When you action one, strike it and note the commit._

| Deferred item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | From → To            | Why deferred                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------- | ----------------------------------------------------------------------------------------------------------------- |
| ~~`shape` derived property on the canvas leaf docs — `tile`/`ambient-light`/`ambient-sound`/`drawing`~~ **DONE (5.6e):** typed `shape`/`_shape` members added to all four docs (imported from `#client/data/shapes.mjs`); inline `// TODO(v14)` markers removed.                                                                                                                                                                                                                                                                                                                           | **4 → 5.6e**         | **Resolved.** `client/data/shapes.d.mts` (5.6d) provided the client subclasses.                                   |
| Canvas leaf-layer **5.6g** FIXMEs: `static paletteClass` on `notes`(`NotePalette`)/`walls`(`WallPalette`)/`drawings`(`DrawingPalette`)/`lighting`(`AmbientLightPalette`)/`sounds`(`AmbientSoundPalette`)/`tiles`(`TilePalette`)/`regions`(`RegionPalette`) (inline `// FIXME: <X>Palette` in each) + the `paletteCreateData` getter return in `placeables-layer.d.mts`. Add the `static paletteClass` members once the `applications/sheets/palette/` classes exist. ~~`RegionPlacementOptions` typedef ref in `layers/_types.d.mts`~~ **DONE (5.6f)** — `= RegionLayer.PlacementOptions`. | **5.5b/5.6f → 5.6g** | Palette apps live in `applications/sheets/palette/`, authored in 5.6g (feeds `paletteClass`/`paletteCreateData`). |
