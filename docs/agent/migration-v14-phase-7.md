# Phase 7 — Greenfield (`client/canvas` vfx, `client/data`, `common`) — detail

> Per-phase detail for the v14 migration. Slim tracker: [migration-v14.md](migration-v14.md). Closed phases: [migration-v14-archive.md](migration-v14-archive.md).

**Status:** In progress. **Risk:** Medium–High (authoring from scratch + the most entangled subsystems).

## Progress

- **2026-05-28 — `canvas/vfx/` subsystem AUTHORED (all 27 files), CI green.** Greenfield-authored the full v14 VFX framework under `src/foundry/client/canvas/vfx/`: foundation (`vfx-constants`, `_types` [VFXComponentAnimation/VFXPathGenerator/VFXBasePathPoint/VFXPathPoint/VFXPositionalSoundData], `vfx-utils`, `vfx-path`, `vfx-canvas-container`), the 5 `fields/` (VFXPointField, VFXReferenceField + Object/Point subclasses, VFXPointSourcePolygonField), the 3 `animations/`, the 2 `paths/`, the 2 core DataModels (`VFXComponent`, `VFXEffect`), and the 6 `components/` (Shake, ScrollingText, PositionalSound, ParticleGenerator, SingleImpact, SingleAttack). Wired `export * as vfx` into `client/canvas/_module.d.mts` and added the `CONFIG.Canvas.vfx` registration surface (`enabled`/`components`/`animations`/`paths`) to `config.d.mts` (`CONFIG.Canvas.VFX` interface). Added `tests/foundry/client/canvas/vfx/vfx.test-d.ts`. All 5 gates green.
  - **Pragmatic fidelity (deliberate, given the EXPERIMENTAL/non-stable framework):** `animejs` is not a typed dep, so `VFXComponent#timeline` is `VFXComponent.Timeline = unknown`; `VFXReferenceField` and its subclasses are modeled with loose generics (the reference-resolution types aren't threaded); signature-unchanged protected overrides (`_draw`/`_stop`/`_load`/`_attach`) are omitted per repo convention; the dynamic CONFIG-driven `VFXEffect#components` schema field uses a pragmatic `ObjectField` with an overridden InitializedType (`Record<string, VFXComponent.Any>`). These are reasonable for a subsystem Foundry says will change; tighten if/when it stabilizes.
- **2026-05-28 — `canvas/vfx/` scope finding: keep deprioritized (Foundry marks it EXPERIMENTAL/non-stable).** Surveyed the v14 source: 27 files / ~2984 lines (none yet in the repo), with a `CONFIG.Canvas.vfx` registration surface (boundary-touching). The `_module.mjs` `activate()` logs that this is _"the Foundry VTT version 14 EXPERIMENTAL VFX framework … Its classes, functions, and configuration are likely to change over coming releases. The entire VFX module should be treated as non-stable regardless of the stability designation of the release build."_ Authoring full types now models an explicitly-unstable, zero-consumer subsystem that Foundry says will change — low value, high churn risk. **Recommend leaving vfx for a dedicated effort once Foundry stabilizes it; it should not gate Phase 7's high-value completion.**
- **2026-05-28 — Level loose-ends in source-polygon: DONE, CI green.** Now that the `Level` document exists, tightened `PointSourcePolygon`: added the `level?: Level.Implementation` config property to `Config._InexactConfig` (v14 `PointSourcePolygonConfig.level`, defaults to viewed Level) and the `get level(): Level.Implementation` / `get scene(): Scene.Implementation` accessors. This unblocks `ClockwiseSweepPolygon`'s inherited `this.level`/`this.level.id`/`this.level.edges` usage and aligns `RegionDocument._computeShapeConstraint`'s `PointSourcePolygon.Config`. Extended `clockwise-sweep.test-d.ts`. (`surfaceExposure`/`ElevatedSurfaceExposureOptions` config prop + the elevated-surface-exposure-polygon return type remain a separate informational item.)
- **2026-05-28 — MeasuredTemplate reconciliation: PARTIAL (safe parts landed), structural un-embed BLOCKED → needs human-reviewed architecture. CI green.** Attempted the full reconciliation (un-embed from Scene + remove `MeasuredTemplate` from `EMBEDDED_DOCUMENT_TYPES`). **Hard blocker:** v14 has `MeasuredTemplate` in neither `PRIMARY_DOCUMENT_TYPES` nor `EMBEDDED_DOCUMENT_TYPES`, so it is genuinely absent from `ALL_DOCUMENT_TYPES` — yet it stays a `Document` subclass (deprecated) with `CONFIG.MeasuredTemplate`. The repo requires every `Document`'s name ∈ `ALL_DOCUMENT_TYPES` and every `documentConfiguration` entry to be a valid type; removing it cascades into `Document`'s `DocumentName` constraint, the whole `documentConfiguration` map, the `parentCollection` union, AND the canvas `AnyCanvasDocument`/Levels constraints (MeasuredTemplateDocument lacks `levels`). Faithfully modeling a "deprecated, non-registered `Document` subclass" is an **architectural change to `document.d.mts` + `documentConfiguration.d.mts` + `src/configuration/` boundary files requiring human design** — reverted the structural edits to keep CI green.
  - **Landed (safe, additive, non-structural):** consumer-visible `@remarks Deprecated since v14` prose on `MeasuredTemplateDocument`, canvas `TemplateLayer`, and the `MeasuredTemplate` placeable (per the anti-pattern, prose not a `@deprecated` tag); the new v14 `static MeasuredTemplateDocument._fromRegion(region)`. The v14 shim DB ops (`createDocuments`/`updateDocuments`/`deleteDocuments` delegating to Region) need no type-level change — same signatures. Extended `measured-template.test-d.ts`. **Still deferred:** the structural un-embed + type-system removal (see deferral row).
- **2026-05-28 — token Tier-B giant: DONE (member-diff), CI green. ALL 4 Tier-B giants now done.** Member-diffed `client/documents/token.d.mts` (the token-movement `_types` were already largely authored in prior batches, so this was a true member-diff, not greenfield). Set-diffed the v14 source's class members against the declaration and added the ~22 genuinely-missing members: `get scene`; `_returnedMovementPromises`/`attachments` properties; `get isLazyDelta`/`_forceDeltaActor`/`_onDeltaMaterialized`; `includedInLevel`; `prepareData` override; `_prepareBars`; `startMovement`; `getOccupiedGridSpaceOffsets`; `_onMovementPlanned`; the origin family (`getMovementOrigin`/`getLightOrigin`/`getVisionOrigin`/`getSoundOrigin`/`getListenerPosition` → `ElevatedPoint`); the test-point family (`getContainmentTestPoints`/`getVisibilityTestPoints`/`getOcclusionTestPoints` → `Point[]`/`ElevatedPoint[]`, `_constrainTestPoints`); and the active-effect application family (`applyActiveEffects`/`_renderActiveEffectChanges`/`_onOverrideSize`/`_getReplacementData`). Per repo convention, the signature-unchanged operation/document inheritDoc overrides (`_onCreate`/`_preUpdate`/`_onUpdate`/`_onDelete`/`_pre*Operation`/`_on*Operation`) stay omitted (an in-file comment already documents this). Extended `tests/foundry/client/documents/token.test-d.ts`. All 5 gates green.
  - **Used existing partial types** (`Dimensions3D`/`Dimensions2D`) for the origin/test-point `data` params, matching how `getCenterPoint`/`testInsideRegion` are already typed. **Known residual drift (not fixed here):** the repo's `TokenDocument.Position` lacks v14's `depth`/`level` fields (Scene-Levels); fixing that ripples through the movement-waypoint types and is its own follow-up. `toggleActiveEffect` (removed in v14) is already flagged `@deprecated` in-file → P8 prune. The stale `_types.d.mts` aliases `TokenMeasureMovementPathWaypoint`/`TokenRegionMovementWaypoint` (no longer real v14 typedefs) left as informational `unknown`.
- **2026-05-28 — region Tier-B giant: DONE (member-diff), CI green.** Authored the `RegionSurface` prerequisite (`RegionDocument.Surface` interface + `RegionSurface` alias in `client/documents/_types.d.mts`) and member-diffed `client/documents/region.d.mts` against the v14 source. Added the missing live members: getters `isSingleShape`/`clipperPolyTree`/`area`; methods `clampElevation`, `_createClipperPolyTree`, `updateShapeConstraints`, `_computeShapeConstraints`, `_computeShapeConstraint` (returns `PointSourcePolygon`), `_onGridChange`, `_clearPolygonTree`, `_onPolygonTreeChange`, `removeShapeDialog`; static `createTokenEmanation`; `teleportToken` (added `options`), `teleportTokens`, `spawnTokens`; and the Deprecations block (`regionShapes` now `@deprecated since v14` + the live `_regionShapes` getter). New namespace types: `Surface`, `UpdateShapeConstraintsOptions`(+`_`), `_TokenPlacementOptions`, `TeleportTokenOptions`, `TeleportTokensOptions`, `SpawnTokensOptions`, `CreateTokenEmanationOptions`, `TokenPanningOptions` (latter duplicated pending token-giant unification). The runtime `attachment.token` resolution (id→document in `prepareBaseData`) is modeled by overriding the `attachment.token` `ForeignDocumentField` InitializedType to `TokenDocument.Implementation | null`. **Dropped the stale `_updateTokens` shim** (+ orphaned `_UpdateTokensOptions`/`UpdateTokensOptions`). Per repo convention, signature-unchanged inheritDoc overrides (`clone`, `_onCreate`/`_onUpdate`/`_onDelete`, `_updateCommit`, `_refreshViewedState`, static `_pre*`/`_on*Operation`) are omitted (inherited). Added `tests/foundry/client/documents/region.test-d.ts`. All 5 gates green.
  - **Did NOT** do the polygon-tree file-move (`region-shapes/polygon-tree` → `data/polygon-tree`, `RegionPolygonTree`→`PolygonTree`) nor the client-data shapes barrel unification — kept the existing `RegionPolygonTree`/`regionShapes` types (structurally identical). Those remain deferred (see shapes-barrel deferral row) and can ride a later cleanup; they do not block region's member surface.
- **2026-05-28 — `Level` document authored (commit `6632620a0`), CI green.** `BaseLevel` (`common/documents/level.d.mts`) + `Level` (`client/documents/level.d.mts`, via `ClientDocumentMixin`), full v14 schema (`elevation`/`background`/`foreground`/`fog`/`textures`/`visibility.levels`/`sort`/`flags`), client members (`index`, `isView`, `isVisible`, `edges`, `prepareBaseData`, `clampElevation`). Added `SceneLevelsSetField` to `common/data/fields.d.mts` and the v14 `TEXTURE` file category (`TEXTURE_FILE_EXTENSIONS` + `FILE_CATEGORIES.TEXTURE`). Registered across every surface: `EMBEDDED_DOCUMENT_TYPES`, `documentConfiguration.d.mts` (8 sites + `MisconfiguredLevel`), `CONFIG.Level`, common/client `_module` barrels, `globals.d.mts`, `client.d.mts`, eslint `documents` map, `Document.DropDataFor`. Embedded into the Scene schema as `levels` (+ `Scene.Metadata.Embedded`/`DirectDescendantName`/`DirectDescendant`/`DirectDescendantClass`). `tests/foundry/client/documents/level.test-d.ts` added.
  - **Note:** `Level`'s runtime-derived `elevation.base` (set in `prepareBaseData`) is not modeled in the schema's `InitializedData` — minor, refine later.
  - **Did NOT** remove `MeasuredTemplate` from `EMBEDDED_DOCUMENT_TYPES`/Scene `templates` (v14 source removed both) — that is the deferred MeasuredTemplate reconciliation; left intact to avoid the cascade.
- **2026-05-28 — per-placeable Scene Levels fields (commit `ed5eb4a0d`), CI green.** Replaced the `TODO(v14-levels)` schema stubs with real fields: `levels: SceneLevelsSetField` on Tile, Note, Drawing, AmbientLight, AmbientSound, Region, Wall (Wall was missing it); `level: DocumentIdField` on Token (persisted `Schema` with an `initial`, and `MeasuredMovementWaypointSchema` without — per `#defineMovementFields` `noInitials`). Drawing's stub was repositioned after `elevation` to match source order.
- **2026-05-28 — `common/data/operators` authored (commit `0e533917e`), CI green.** `DataFieldOperator` + `OPERATOR_VALUE`/`OPERATOR_IDENTIFIER` + `ForcedDeletion`/`ForcedReplacement` + `reconstructOperator`, re-exported as `foundry.data.operators`. This is the `operators.mjs` prerequisite the scene/region/token giants needed for their `DataFieldOperator` usage — **that blocker is now cleared.**
- **2026-05-28 — 4 missing region-behavior data models authored (commit pending in this session), CI green.** `ApplyActiveEffect`, `ChangeLevel`, `DefineSurface`, `ModifyMovementCost` RegionBehaviorType subclasses (the only ones missing vs v14 source); wired into the region-behaviors `_module`. Each has a clean type surface (subsystem coupling lives only in private methods); `ModifyMovementCost._getTerrainEffects` is loosely typed pending token-movement `_types`. **`data/region-behaviors/` greenfield is now complete.**
- **2026-05-28 — active-effect Tier-B giant: prerequisites done (commits `e957ef12c`, + ActiveEffectRegistry), CI green.** Authored `ActiveEffectRegistry` (`client/helpers/active-effect-registry.d.mts`, extends `IterableWeakSet<ActiveEffect>`; barrel-wired as `foundry.helpers.ActiveEffectRegistry`) and the `CONFIG.ActiveEffect` additions (`changeTypes` + new `CONFIG.ActiveEffectChangeTypeConfig`, `expiryEvents`, `expiryAction`, `phases`; handler/render callbacks loosely typed). Additive members landed (commit `6f3d02449`): the static `registry`, `isExpiryTrackable`, `isExpiryEvent` (+ `IsExpiryEventContext`), and the `context` param on `updateDuration`.
- **2026-05-28 — active-effect Tier-B giant: DONE (the risky core), CI green.** Completed the change-application instance→static refactor in `client/documents/active-effect.d.mts`:
  - **Change-application family is now static** with v14 signatures `(targetDoc, change, options?)`: `applyChange`, `applyChangeField`, `_applyChangeUnguided`, `_replaceDataRefs`, and `_applyChange{Add,Subtract,Multiply,Override,Upgrade,Custom}` (note v14 added `_applyChangeSubtract` and dropped the separate `_applyChangeDowngrade` — upgrade handles both). Added new namespace types `ChangeTarget` (= Actor|Item|TokenDocument impl), `ApplyChangeOptions`, `ApplyChangeFieldOptions`.
  - **v13 instance methods are now `@deprecated since v14, until v16` shims** grouped in a "Deprecations and Compatibility" block at class end: `apply`, static `applyField` (kept its `<Field>`/`ApplyFieldReturn` generic for ergonomics), `_applyLegacy`, `_applyAdd`/`_applyMultiply`/`_applyOverride`/`_applyUpgrade`/`_applyCustom`, static `getInitialDuration`. Dropped the v13-only `_castDelta`/`_castArray`/`_parseOrString` (now truly-private `#` in source).
  - **`ActiveEffectDuration` rework:** `Duration` now models v14's `EffectDurationData & _ActiveEffectDuration` (`seconds: number|null`, `remaining`, `secondsRemaining?`, `label`, `_worldTime?`, `_combatTime?`) with the v13 `type`/`duration` members kept as `@deprecated` (they are runtime getters added by `updateDuration`). `_prepareDuration` re-signed to `(duration?, context?) → Duration`; added `_prepareTimeBasedDuration`/`_prepareCombatBasedDuration`; **removed** the v13-only `_requiresDurationUpdate`/`_getCombatTime`/`_getDurationLabel` (gone in v14).
  - **`ChangeData` migrated** off v13's numeric `mode` to v14's `type: string` + `phase: string` (+ optional `effect`, `priority: number|null`); maps Foundry's `EffectChangeData`/`ActiveEffectChangeData`.
  - **Tightened the loose CONFIG callbacks:** authored `ActiveEffect.ChangeHandler`/`ChangeRenderer` (+ `ChangeRendererContext`, `ChangePhaseConfig`) and wired them into `CONFIG.ActiveEffectChangeTypeConfig.handler`/`.render` (the FIXMEs there are gone). Added the static `CHANGE_PHASES`/`CHANGE_TYPES`/`EXPIRY_EVENTS` getters, static `getEffectStart` (+ `EffectStartData`; `getInitialDuration` return reshaped to `{ start }`), and the missing `actor`/`item`/`thumbnail` getters. Added `ActiveEffectChangeData`/`ActiveEffectChangeHandler`/`ActiveEffectChangeRenderer` to `client/documents/_types.d.mts`. Test cascade fixed in `active-effect.test-d.ts` (deprecation shims fenced with `no-deprecated` disables). All 5 gates green. **Remaining for active-effect: a final member-order verification pass** (the touched methods are correctly signed but some pre-existing members sit in v13 order) — low-risk polish.
- **2026-05-28 — scene Tier-B giant: SUBSTANTIALLY COMPLETE** (commits `4773af838`, `91ce464d3`, `4a8b7df8f`, `008ce71c3`), CI green. Done: the Levels/edges/surfaces member batch; the `transition` schema field; the **`background`/`foreground`/`backgroundColor`/`foregroundElevation` schema→`@deprecated`-getter migration** (v14 moved these to the Level document — removed the schema fields, added the deprecated `firstLevel`-deriving getters + `DeprecatedBackgroundData`, fixed the consumer/test cascade); and the **`initialLevel` schema field** (DocumentIdField with InitializedType overridden to `Level.Implementation | undefined` to coexist with its shadowing getter). **Only remaining:** a final exhaustive member-order verification pass against v14 source (low-risk polish).
- **2026-05-28 — scene Tier-B giant, first batch (commit `4773af838`), CI green.** `[~]` partial. Added the Scene-Levels + edges/surfaces members now unblocked by `Level` + `operators`: fixed `_view` drift (`boolean`→`string | null`), `_availableLevels` cache, `availableLevels`/`initializedEdges` getters, `prepareEmbeddedDocuments`/`prepareDerivedData`, `updateRegionShapeConstraints`(+`_`), `cycleLevel`, `_configureLevelTextures`, `_resetEdges`, `initializeEdges`, `_getAvailableLevels`, `moveTokens` (loose, token-movement), `_invalidateSurfaces`/`getSurfaces`/`testSurfaceCollision` (loose `RegionSurface`; new `GetAvailableLevelsOptions`/`GetSurfacesOptions`/`TestSurfaceCollisionConfig`). Also added `initialLevel`/`firstLevel` to common BaseScene (commit `ba1db3b5b`). **Scene-giant remaining:** the `background`/`foreground`/`backgroundColor`/`foregroundElevation` **schema→`@deprecated`-getter migration** (v14 moved these fields to the `Level` document — a schema removal with consumer/test cascade), `_configure`/`_updateCommit` inheritDoc overrides (omitted per repo convention), static `_onUpdateOperation`, and a final full member-order verification.
- **2026-05-28 — tightened the `object`-typed Level FIXMEs (commit `e244d7bfa`), CI green.** Replaced loose `object` with `Level.Implementation` in: `CanvasEdges` (ctor + `get level()`), `BaseEffectSource#level`, `PointLightSource#_canDetectObject`, `DetectionMode#_canDetect`, `Canvas#level`/`#inferLevelFromElevation`/`TearDownOptions.nextLevel`, `SceneManager#_getAvailableLevels`, `SceneConfig#defaultLevel`/`_onSortLevel`. Updated the matching `.test-d.ts`. **Still loose/deferred:** `PointSourcePolygon.Config.level` + `ClockwiseSweepPolygon` level internals (source-polygon not yet touched), `CanvasOcclusionMask#occludedSurfaces` (`RegionSurface`, region giant), the primary/visibility group `levelTextures`/`surfaceExposure` (informational TODOs — already concrete types).

## Scope

Greenfield + the deferred subsystems that everything else has been parking here:

1. **New `canvas/vfx/` subsystem** (~21 files) — no v13 analog, no current consumers. Intentionally last. _Per the [scope-priority decision](migration-v14.md#scope-priority), this is **deprioritized** — do only if explicitly requested._
2. **`data/region-behaviors/`** (~4 files) — region behavior data models.
3. **The v14 Scene Levels subsystem** — the big one (see inherited deferral below). New `Level` document + `BaseLevel`, `SceneLevelsSetField`, `LevelData`/`LevelTexture` typedefs, the Scene rework, and the `levels`/`level` fields scattered across Scene/Token/ambient-light/ambient-sound/drawing/note/region/tile/wall. Also the missing `client/data/polygon-tree.d.mts` (`PolygonTree`).
4. **The MeasuredTemplate → client deprecation** (see inherited deferral).
5. **The 4 deferred `client/documents` Tier-B giants** — active-effect, region, scene, token (see inherited deferral). Author each one's prerequisite, then its member-diff, in one session.
6. **The client-data shapes barrel re-export** (the shadow — see inherited deferral). Rides with the region giant + Scene Levels.
7. Remaining missing files not absorbed by earlier phases.

## Sequencing note

Scene Levels gates 3 of the 4 giants (scene/region/token) — author the `Level` document + `common/data/_module` `operators` re-export **first**, then the giants become member-diffs against a real subsystem instead of stubs. active-effect is independent (its own registry/config/duration prerequisites).

## Inherited deferrals (action these in Phase 7)

_Folded in from the old central deferrals table. When you action one, strike it and note the commit._

| Deferred item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | From → To    | Why deferred                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MeasuredTemplate reconciliation: move `BaseMeasuredTemplate` common→client as `@deprecated since v14`, drop common `_module`/`_types` exports + the `MeasuredTemplateData` typedef, un-embed from Scene. **P4 finding:** the _entire_ v14 client `measured-template.mjs` IS this reconciliation — `BaseMeasuredTemplate` is now declared in the client file (deprecated), and `MeasuredTemplateDocument` is a deprecated shim whose static `createDocuments`/`updateDocuments`/`deleteDocuments` delegate to `RegionDocument` (+ `_fromRegion`/`#migrateData`). So **all of `client/documents/measured-template.d.mts` is P7 work**, not just the move — there is no Phase-4-separable member surface. **P5.5b finding:** the canvas `layers/templates.d.mts` (`TemplateLayer`) is also `@deprecated since v14` in source — its v14 **member surface is done** (Batch 5.5b), but the **class-level `@deprecated` annotation is deferred here too** (it cascades into the `src/configuration/` boundary `globals.d.mts`, `config.d.mts`, `client.d.mts`, the interface-group `templates` accessor, and 4 tests; must land coherently with this reconciliation). **P5.6b finding:** the canvas `placeables/template.d.mts` (the `MeasuredTemplate` placeable) is **also** `@deprecated since v14` in source — its v14 **member surface is done** (Batch 5.6b), but the **class-level `@deprecated` annotation is deferred here too** (same cascade: `src/configuration/` globals/config/client + `TemplateLayer` + tests; a `@privateRemarks` note marks it in-file).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **3 → 7**    | Cascades `no-deprecated`/missing-type errors into un-migrated Scene (P7), client MeasuredTemplate, canvas (P5). Travels with the Scene embedded-collection rework.                                                                                                                                                      |
| **client/documents Tier-B giant member-diffs** — ~~`active-effect`~~ (**DONE 2026-05-28**), ~~`region`~~ (**DONE 2026-05-28** — polygon-tree move + shapes-barrel unification still deferred separately), ~~`scene`~~ (substantially done), ~~`token`~~ (**DONE 2026-05-28** — ~22 missing members added; residual `Position` depth/level drift deferred). **ALL 4 GIANTS DONE.** Each is **NOT a pure member-diff**; each needs prerequisites that don't exist yet, so author it **prerequisite-first then member-diff, in one session**. **Blockers:** ~~`active-effect` (1154) — new `ActiveEffectRegistry`, `CONFIG.ActiveEffect` config additions, `ActiveEffectDuration` rework, change-application moved instance→static~~ (done). `region` (2718, ~25 missing live members: `isSingleShape`/`clipperPolyTree`/`area`, `attachment`, `clone`, `clampElevation`, `_createClipperPolyTree`, `updateShapeConstraints`, `_computeShapeConstraint(s)`, static `_pre*`/`_on*Operation`, `_updateCommit`, `_on{Create,Update,Delete}`, `_onGridChange`, `_clearPolygonTree`, `_onPolygonTreeChange`, `_refreshViewedState`, `createTokenEmanation`, `teleportTokens`, `spawnTokens`, `removeShapeDialog`, `_regionShapes`; drop the stale `_updateTokens` decl — v14's is private `#updateTokens`) — Scene Levels (`this.parent.levels`, `level` params) + `DataFieldOperator`. `scene` (1780) — it IS the Levels container (~110 refs) + `DataFieldOperator`. `token` (4009) — the new token movement subsystem (~15 `TokenMovement*` `_types` typedefs + `TokenConstrainMovementPathOptions`) + `DataFieldOperator` + Levels (78 refs). **P5.6c finding:** the Token **placeable** (`canvas/placeables/token.d.mts`) movement/drag surface is **also deferred here** — its non-movement v14 surface is done (Batch 5.6c part B, file left `[~]`), but `planMovement` / the `_getDrag*`/`_addDragWaypoint`/`_changeDragElevation`/etc. drag family / `recalculatePlannedMovementPath` / `createTerrainMovementPath` / `panCanvas` / keyboard+HUD+config movement helpers / animation-movement-speed helpers / `static _getDropActorPosition` / the v13-leftover `_onDragLeftStart` all need the movement `_types` and ride with this giant.                                                                                                                                                                                                                                                                                                | **4 → 7**    | Pattern 9: "leaf" docs with missing-file/subsystem prerequisites. Scene Levels + `operators.mjs` gate 3 of 4; active-effect has its own registry/config/duration. Authoring now = typing against stubbed `levels` fields / a missing `Level` document / a missing `operators.mjs` (the trap avoided for active-effect). |
| Scene Levels subsystem: new `Level` document + `BaseLevel`, `SceneLevelsSetField` (extends `SetField`), `LevelData`/`LevelTexture` typedefs, the Scene rework, and the `levels`/`level` fields on Scene/Token/ambient-light/ambient-sound/drawing/note/region/tile/wall (currently stubbed `// TODO(v14-levels)`). **Incl. (flagged by P5.1) canvas geometry consumers:** `CanvasEdges` constructor `level` param + `get level()` (FIXME-typed `object` in `edges/edges.d.mts`); `ClockwiseSweepPolygon.Config.level` + its `this.level.id` intersection internals; `PointSourcePolygon` `get level()`/`get scene()`/`surfaceExposure` + `Config.level` (source-polygon not yet touched). **Also missing file `client/data/polygon-tree.d.mts`** (`PolygonTree`) — return type of `geometry/shapes/elevated-surface-exposure-polygon` (FIXME `object\|null`) and the edge intersection tree; author in P7 client/data then drop those FIXMEs. **Incl. (flagged by P5.3) canvas sources/perception consumers:** `BaseEffectSource#get level()` (FIXME `object` in `sources/base-effect-source.d.mts`; backing `#level`); `PointLightSource#_canDetectObject(target, level)` + `DetectionMode#_canDetect(..., level)` `level` params (FIXME `object`); `DetectionMode._testCollision`'s `test.level.isView` usage. (`BaseEffectSourceData.level` is a plain id string — already typed `string \| null`, not blocked.) **Incl. (flagged by P5.5a) canvas groups/layers consumers:** `PrimaryCanvasGroup#levelTextures` (`PrimarySpriteMesh[]` — typed, but populated by Levels) + its `@deprecated` `hoverFadeElevation` get/set (reads `canvas.level`); `CanvasVisibility#surfaceExposure` + `level` (FIXME-typed `object`) on `CanvasVisibility.TestConfig`/`Test`; `PlaceablesLayer#viewedDocuments`/`getDocuments`(deprecated)/`deleteAll` (TODO(v14-levels), no type blocker — `doc.viewed`/`canvas.scene.levels`). **Incl. (flagged by P5.7) the Canvas god-object + SceneManager:** `Canvas#get level()` (FIXME `object`; backing `#level`), `Canvas#edges` getter (derives `this.level?.edges`), `Canvas#inferLevelFromElevation` (return `object\|null`, `options.levels: Set<string>`), `Canvas.TearDownOptions.nextLevel` (FIXME `object`), `Canvas#_viewOptions`/`Canvas.SceneViewOptions.level` (a plain id string — not blocked), `SceneManager#_determineInitialLevel` (returns a level-id string — not blocked) + `SceneManager#_getAvailableLevels(Set<object>)` (FIXME `Set<Level>`). Drop these FIXMEs when `Level` exists. | **3 → 7**    | New v14 subsystem entangled with the greenfield `Level` document. `common/data/_module.d.mts` `operators` re-export rides along (operators.mjs not authored yet).                                                                                                                                                       |
| Canvas leaf-layer **P7** FIXMEs (Batch 5.5b): `tokens.d.mts` `_movementPlanningContext`/`_placementContext` (FIXME-typed `AnyObject \| null`), `_updatePlannedMovements` values (`TokenPlannedMovement`), `placeTokens` options callbacks â all the **token-movement subsystem** (the same one the Tier-B `token` giant needs). Plus occlusion `CanvasOcclusionMask#occludedSurfaces` element type (`DeepReadonly<RegionSurface>`, FIXME-typed `object`). Drop the FIXMEs when the token-movement types / `RegionSurface` (`client/documents/_types`) exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **5 → 7**    | Token-movement subsystem + greenfield region `RegionSurface` are P7; only the consuming canvas members were reachable in 5.5b.                                                                                                                                                                                          |
| **Scene-Levels precision in Phase 6 sheets** (2026-05-28): `scene-config` and `PlaceableConfig` are migrated + CI-green, but their Level-coupled members are typed `object` with `// FIXME … → P7` pending the `Level` document — `scene-config`'s `get defaultLevel()`, `_getLevelContextOptions()`, `_onSortLevel(event, level)`; `PlaceableConfig`'s body-level `selectableLevels` coupling (its type surface is already concrete). Tighten these when `Level` lands (same authoring as the Scene Levels row above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **6 → 7**    | The `Level` document is greenfield P7; the configs only needed loose typing to ship CI-green now.                                                                                                                                                                                                                       |
| **Client-data shapes barrel re-export (the shadow)** (Batch 5.6d): the v14 source `client/data/_module` additionally re-exports the 10 client shape classes from `./shapes.mjs` under the SAME names as the common `BaseShapeData` subclasses already pulled in by `export * from "#common/data/_module.mjs"`. At the type level this would SHADOW `foundry.data.RectangleShapeData` (etc.) with the namespace-less client subclass — dropping the common class's `.Schema`/`.Source`/`.CreateData`/… namespaces AND breaking `RegionDocument#shapes` typing (built from the common `BaseShapeData.Types`, which maps to the common classes). `shapes.d.mts` is authored standalone (importable from `#client/data/shapes.mjs`); a `// TODO(v14, P7)` in `client/data/_module.d.mts` marks the omitted re-export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **5.6d → 7** | Adding it coherently needs the client subclasses to re-expose the common `.Schema`/… namespaces AND `region.shapes` to resolve to the client subclasses — a cross-cutting region/Scene/common-`Types` unification (rides with the region Tier-B giant + Scene Levels).                                                  |
