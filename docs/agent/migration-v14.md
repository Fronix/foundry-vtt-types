# v14 Migration Plan & Tracker

## When to consult this file

This is the **single source of truth** for the Foundry v13 → v14 type migration: the phased roadmap, the per-file status, and the definition of "done". Consult it before starting any v14 migration work, and update it as files are completed.

Ground truth for every decision below is the Foundry v14.363.0 source at `/home/fronix/git/foundry/resources/app/` (`client/`, `common/`, `public/scripts/`). When the source disagrees with this plan, the source wins — fix the plan.

> **Closed phases live in the archive.** Completed phases' full detail (per-file checklists, drift surveys, commit lists, reusable findings) is moved to **[migration-v14-archive.md](migration-v14-archive.md)** so this tracker stays small enough to load every session. Nothing is deleted — read the archive on demand. This file owns what is **open**; the archive records what is **closed**.

## Definition of "done" (the per-file bar)

A file counts as **migrated to v14** only when all three hold:

1. **Source-diffed** — its members (order, signatures, additions, removals) have been compared against the corresponding v14 `.mjs` file. Member order matches the source (per [conventions.md](conventions.md)).
2. **CI-green** — passes the full gate: `npm run typecheck` (tsgo), `npx tsc --exactOptionalPropertyTypes false`, raw `npx tsgo`, `npm run lint`, `npm run test-types`.
3. **Type-tested** — a `.test-d.ts` exercises the changed/added surface (extend the existing one or add a new mirror under `tests/foundry/`).

CI-green alone is **not** sufficient — CI does not know the v14 source, so silent v13 drift survives it. The diff is what catches drift.

## Sequencing principle

**Foundation-up by layer.** Lower layers are verified before the layers built on them: `common/abstract` → `common/documents` → `client/documents` → `client/canvas` → `client/applications` → greenfield (`vfx`, region-behaviors) → cleanup. The new `canvas/vfx/` subsystem (no v13 analog, no current consumers) is intentionally last.

## Branch & merge strategy (applies to every phase)

**All v14 work lives on a single long-running `v14` branch (off `main`). Commit every phase directly onto `v14` — do NOT cut per-phase branches.**

> History note: Phases 1–3 were originally done on stacked per-phase branches (`v14/createDialog-stored` → `v14/remove-temporary` → `v14/verify-common`). On 2026-05-26 those were collapsed into a single `v14` branch (the stack was already linear, so nothing was lost; the old labels were deleted, their commits remain in `v14`'s history with `(Phase N)` in the messages). The stacked scheme was abandoned because the only thing it bought was per-phase PR granularity, which isn't worth the ceremony on a solo fork.

The stack was always linear (each phase only ever appended commits), so there was never a merge-**conflict** problem to solve — `v14` is simply that linear history under one name. When the migration (or a chosen checkpoint) is ready, merge `v14` into `main` in one fast-forward. `main` does not drift independently (fork-only), so the merge stays conflict-free.

**Remote:** push only to `origin` (the Fronix fork) — never `upstream` (League). As of the collapse, `v14` is local-only; the stale `origin/v14/createDialog-stored` (the old Phase-1 push) can be deleted whenever convenient.

## The migration landscape (baseline, 2026-05-25)

Measured against v14.363.0. Directory structure already mirrors v14 1:1 — **no restructuring needed**; this is a content-accuracy migration.

| Category                                          | Size                                                                   | Concentration                                                                                                                                                                           |
| ------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A. Missing files** (in v14 source, absent here) | 97                                                                     | new `canvas/vfx/` (~21), placeable UI: `applications/sheets/palette/` (8), `applications/sidebar/tabs/` (10), `.../filters/` (4); `data/region-behaviors/` (4); misc canvas/elements/ux |
| **B. Stub files** (`@remarks TODO: Stub`)         | 65                                                                     | ~98% in `applications/` (HUD, sidebar, sheets, settings menus); 2 in `data/`                                                                                                            |
| **C. In-file drift**                              | ~34 docs (createDialog `.Stored`) + ~200 `Temporary` sites + scattered | `client/documents/`, `common/abstract/document.d.mts`, canvas                                                                                                                           |

Files in the repo but not in v14 source (19) are almost all barrel/index files and renames (`quad-tree`→`quadtree`, `region-legend` removed, `measured-template` moved) — handled opportunistically within the relevant phase, not a category of their own.

---

## Roadmap

| Phase | Scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Layer                                  | Risk                                                    | Status                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | `createDialog` v14 signature: return `.Stored \| null`, add 4th `renderOptions` param (34 docs + mixin)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | client/documents                       | Low (bounded, mechanical)                               | **Done — CI green**                                                                                                                                                                                                                                                                                                                                                     |
| **2** | Remove the `Temporary` create-operation concept entirely (~200 sites; v14 dropped `temporary` from `DatabaseCreateOperation`)                                                                                                                                                                                                                                                                                                                                                                                                                                                            | common/abstract (boundary) + documents | **High — touches `document.d.mts`, needs human review** | **Done — CI green** (85 files, +1041/−1872)                                                                                                                                                                                                                                                                                                                             |
| **3** | Verify `common/abstract` (`DataModel`, `Document`, fields) + `common/documents` base schemas against v14                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | common                                 | High (boundary files)                                   | **DONE (modulo documented cross-phase deferrals) — CI green.** `common/abstract` 10/10 (incl. `document.d.mts` verified-clean), `common/data` complete (fields + shapes + validation + DataModelSchemaField), `common/documents` schemas done. Deferred: Scene Levels + **MeasuredTemplate reconciliation** + BaseLevel → Phase 7; wall EDGE rename → Phase 5.          |
| **4** | Verify `client/documents` leaf classes member-by-member to the done bar (Phases 1–2 already touched them)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | client/documents                       | Medium                                                  | **DONE — CI green.** Every client/documents-layer-actionable leaf doc verified: 28 `[x]` + `wall` `[~]` (edge members → P5). The other 5 (the 4 Tier-B giants active-effect/region/scene/token **+** measured-template) are **reassigned to Phase 7** — they need subsystems that don't exist yet, so they are no longer Phase-4 scope; see deferrals. Detail archived. |
| **5** | Canvas: verify existing + fill canvas stubs + add missing canvas files (**excluding vfx**). **Inherits:** wall `EDGE_SENSE_TYPES`/`EDGE_DIRECTIONS` rename + `WallDocument` edge members (from Phase 3 — see deferrals) and the `shape`/`_shape` derived props (from Phase 4 — needs `client/data/shapes.d.mts`).                                                                                                                                                                                                                                                                        | client/canvas                          | Medium                                                  | **ACTIVE — Batches 5.1 (geometry) + 5.2 (rendering) DONE & CI-green; 5.3 (sources + perception) NEXT.** Batched foundation-up into 5.1–5.7 (207 verify + 20 add, 0 stubs). 5.2 surfaced + landed the v14 shader/filter base-API refactor (~60 files, atomic). Level-entangled geometry → P7. See **Phase 5 detail**.                                                    |
| **6** | Applications: fill the 65 stubs (HUD, sidebar, sheets, settings menus) + add missing application files (palette / sidebar-tabs / filters / ux / elements)                                                                                                                                                                                                                                                                                                                                                                                                                                | client/applications                    | Medium (volume)                                         | Not started                                                                                                                                                                                                                                                                                                                                                             |
| **7** | Greenfield: new `canvas/vfx/` (~21 files), `data/region-behaviors/`, remaining missing files, **+ the v14 Scene Levels subsystem** **+ the MeasuredTemplate→client deprecation** (from Phase 3: move `BaseMeasuredTemplate` common→client as `@deprecated since v14`, drop its common `_module`/`_types` exports, un-embed it from Scene — all coordinated with the Scene embedded-collection rework) **+ the 4 deferred `client/documents` Tier-B giants** (active-effect, region, scene, token — author each one's prerequisite, then its member-diff, in one session). See deferrals. | client/canvas, client/data, common     | Medium–High (authoring from scratch)                    | Not started                                                                                                                                                                                                                                                                                                                                                             |
| **8** | Cleanup: remove `@deprecated … removed in v14` types now that we are v14; verify remaining v13-flavored comments; backfill tests; bump `package.json` to `14.x`. **Inherits:** deprecated embedded-collection stub removals (from Phase 3 — see deferrals).                                                                                                                                                                                                                                                                                                                              | repo-wide                              | Low                                                     | Not started                                                                                                                                                                                                                                                                                                                                                             |

Phases are expanded into per-file checklists when they are reached (keeping this doc legible). Closed phases' full detail is archived in [migration-v14-archive.md](migration-v14-archive.md) — see [Closed-phase detail](#closed-phase-detail-archived) below.

---

## Cross-phase deferrals (live)

Work discovered while completing one phase but assigned to a later one. **This table is the authoritative live list — each item MUST be actioned by its receiving phase; do not let it drop.** When you start a receiving phase, fold the matching row(s) into that phase's expanded checklist. (Originating-phase rationale is preserved in the [archive](migration-v14-archive.md); the receiving phase is also flagged in its roadmap row above.)

| Deferred item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | From → To                        | Why deferred                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MeasuredTemplate reconciliation: move `BaseMeasuredTemplate` common→client as `@deprecated since v14`, drop common `_module`/`_types` exports + the `MeasuredTemplateData` typedef, un-embed from Scene. **P4 finding:** the _entire_ v14 client `measured-template.mjs` IS this reconciliation — `BaseMeasuredTemplate` is now declared in the client file (deprecated), and `MeasuredTemplateDocument` is a deprecated shim whose static `createDocuments`/`updateDocuments`/`deleteDocuments` delegate to `RegionDocument` (+ `_fromRegion`/`#migrateData`). So **all of `client/documents/measured-template.d.mts` is P7 work**, not just the move — there is no Phase-4-separable member surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **3 → 7**                        | Cascades `no-deprecated`/missing-type errors into un-migrated Scene (P7), client MeasuredTemplate, canvas (P5). Travels with the Scene embedded-collection rework.                                                                                                                                                      |
| **client/documents Tier-B giant member-diffs** — `active-effect`, `region`, `scene`, `token` class-body verification. Each is **NOT a pure member-diff**; each needs prerequisites that don't exist yet, so author it **prerequisite-first then member-diff, in one session**. **Blockers:** `active-effect` (1154) — new `ActiveEffectRegistry` (`client/helpers/active-effect-registry`), `CONFIG.ActiveEffect` config additions (`phases`/`changeTypes`/`expiryEvents` + `ActiveEffectChangeTypeConfig`), `ActiveEffectDuration` rework; the change-application family moved instance→**static** (`applyChange`/`applyChangeField`/`_applyChange{Add,…}`; v13 instance methods are now `@deprecated until v16` shims). `region` (2718, ~25 missing live members: `isSingleShape`/`clipperPolyTree`/`area`, `attachment`, `clone`, `clampElevation`, `_createClipperPolyTree`, `updateShapeConstraints`, `_computeShapeConstraint(s)`, static `_pre*`/`_on*Operation`, `_updateCommit`, `_on{Create,Update,Delete}`, `_onGridChange`, `_clearPolygonTree`, `_onPolygonTreeChange`, `_refreshViewedState`, `createTokenEmanation`, `teleportTokens`, `spawnTokens`, `removeShapeDialog`, `_regionShapes`; drop the stale `_updateTokens` decl — v14's is private `#updateTokens`) — Scene Levels (`this.parent.levels`, `level` params) + `DataFieldOperator`. `scene` (1780) — it IS the Levels container (~110 refs) + `DataFieldOperator`. `token` (4009) — the new token movement subsystem (~15 `TokenMovement*` `_types` typedefs + `TokenConstrainMovementPathOptions`) + `DataFieldOperator` + Levels (78 refs). | **4 → 7**                        | Pattern 9: "leaf" docs with missing-file/subsystem prerequisites. Scene Levels + `operators.mjs` gate 3 of 4; active-effect has its own registry/config/duration. Authoring now = typing against stubbed `levels` fields / a missing `Level` document / a missing `operators.mjs` (the trap avoided for active-effect). |
| Scene Levels subsystem: new `Level` document + `BaseLevel`, `SceneLevelsSetField` (extends `SetField`), `LevelData`/`LevelTexture` typedefs, the Scene rework, and the `levels`/`level` fields on Scene/Token/ambient-light/ambient-sound/drawing/note/region/tile/wall (currently stubbed `// TODO(v14-levels)`). **Incl. (flagged by P5.1) canvas geometry consumers:** `CanvasEdges` constructor `level` param + `get level()` (FIXME-typed `object` in `edges/edges.d.mts`); `ClockwiseSweepPolygon.Config.level` + its `this.level.id` intersection internals; `PointSourcePolygon` `get level()`/`get scene()`/`surfaceExposure` + `Config.level` (source-polygon not yet touched). **Also missing file `client/data/polygon-tree.d.mts`** (`PolygonTree`) — return type of `geometry/shapes/elevated-surface-exposure-polygon` (FIXME `object\|null`) and the edge intersection tree; author in P7 client/data then drop those FIXMEs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **3 → 7**                        | New v14 subsystem entangled with the greenfield `Level` document. `common/data/_module.d.mts` `operators` re-export rides along (operators.mjs not authored yet).                                                                                                                                                       |
| Deprecated-stub removals: `embedded-collection.d.mts` `update`/`_createOrUpdate`, `embedded-collection-delta.d.mts` `_createOrUpdate` ("removed in v13, warning removed in v14")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **3 → 8**                        | Gone from v14 source; pure cleanup-phase work.                                                                                                                                                                                                                                                                          |
| `TypedObjectField` create-optionality: give it `SchemaField`'s implicit-`{}`-initial so a required `TypedObjectField` isn't wrongly required in `CreateData`, then drop the per-field workaround (`token.detectionModes`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **3 → fields.d.mts improvement** | Candidate `common/data/fields.d.mts` improvement found during token schema work; action when convenient (not phase-gated).                                                                                                                                                                                              |
| `shape` derived property on the canvas leaf docs — `tile`/`ambient-light`/`ambient-sound`/`drawing` assign `this.shape = new <Rectangle\|Circle\|Cone>ShapeData(...)` in `prepareDerivedData`. Declare the typed `shape` member on each once the shape-data types exist. (Inline `// TODO(v14)` left in each file.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **4 → 5/7**                      | Blocked on `client/data/shapes.d.mts` (the `RectangleShapeData`/`CircleShapeData`/`ConeShapeData` classes), a missing `client/data` file not yet authored — author it in the canvas/greenfield phase, then add the four `shape` members.                                                                                |

---

## Closed-phase detail (archived)

Phases 1–4 are **complete and CI-green** (status in the roadmap above; deferrals lifted into the live table above). Their full detail — per-file checklists, drift surveys, commit lists, and reusable type-system findings — lives in **[migration-v14-archive.md](migration-v14-archive.md)**. Consult it when you need closed-phase specifics (which commit landed a change, the exact field rewrites, a reusable gotcha like the `GridOffsetField` dimensions wart).

- **Phase 1** — `createDialog` → v14 signature (`.Stored | null` + 4th `renderOptions` param). 35/35 files. → see the archive's _Phase 1_ section.
- **Phase 2** — remove the `Temporary` create-operation concept (atomic, ~85 files). → archive's _Phase 2_ section.
- **Phase 3** — verify `common/abstract` + `common/data` (fields) + `common/documents` base schemas. → archive's _Phase 3_ section.
- **Phase 4** — verify `client/documents` leaf classes member-by-member. 28 `[x]` + `wall` `[~]` (edge→P5); the 4 Tier-B giants + measured-template reassigned to P7 (see deferrals). 9 batches; patterns (1)–(10). → archive's _Phase 4_ section.

---

## Phase 5 (ACTIVE) — detail

**Scope.** The `client/canvas` layer: (1) **verify** the existing canvas `.d.mts` files member-by-member against their v14 `.mjs` (same done-bar recipe as Phase 4 — diff class bodies, fix drift, member order matches source); (2) **add the 20 missing canvas files** (ex-vfx — see list below); (3) there are **0 canvas stub files** (all 65 stubs are in `applications/`, → Phase 6). The `canvas/vfx/` subsystem (~21 files) is intentionally **excluded** — it is Phase 7.

**Ground truth:** `/home/fronix/git/foundry/resources/app/client/canvas/<path>.mjs` (v14.363.0) ↔ `src/foundry/client/canvas/<path>.d.mts`.

**Batch plan (foundation-up).** 207 verify + 20 add is multi-session; batched like Phase 4. Expand each batch into per-file detail when reached; prune closed-batch detail (→ archive) to keep this section small. Survey refreshed 2026-05-26 (counts below; verify each file against source before editing — counts are guidance).

| Batch   | Scope (verify N existing + add M new)                                                                                                                                                                                                                                                                                                                        | Status                                                     |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------- |
| **5.1** | **geometry/** — verify 11; rename `quad-tree`→`quadtree`; add `shapes/elevated-surface-exposure-polygon`. **Wall EDGE deferral DONE.**                                                                                                                                                                                                                       | **DONE — CI green** (Level-entangled bits → P7; see below) |
| **5.2** | **rendering/** — verified ~89 (shaders/\* incl `lighting/effects` 26, `filters` 13 + `smaa` 4, samplers, weather, vision, region, grid, graphics, `batching` 2, `mixins` 2). 0 new. Landed the v14 shader/filter base-API refactor (coupled across ~60 files, atomic) — see Batch 5.2 detail.                                                                | **DONE — CI green**                                        |
| **5.3** | **sources/ (10) + perception/ (13)** — light/vision/sound sources + detection-modes. 0 new.                                                                                                                                                                                                                                                                  | **NEXT**                                                   |
| **5.4** | **containers/ (17) + primary/ (6)** — add `primary/primary-canvas-particle-container`.                                                                                                                                                                                                                                                                       | [ ]                                                        |
| **5.5** | **groups/ (9) + layers/ (22)** — verify (the `layers/mixins/shapes` new file travels with 5.6).                                                                                                                                                                                                                                                              | [ ]                                                        |
| **5.6** | **placeables/ (22) + the "shape controls" cluster** — add 13 new: `containers/elements/shape-controls`, `placeables/{drawings,lights,regions,sounds,tiles}/shape-controls`, `placeables/{drawings,lights,sounds,tiles}/_module`, `placeables/mixins/shapes`, `layers/mixins/shapes`. **Owns shape derived-props deferral** (new `client/data/shapes.d.mts`). | [ ]                                                        |
| **5.7** | **interaction/ (9) + animation/ (3) + top-level (board, loader, scene-manager, texture-extractor, framebuffer-snapshot) + extensions/ (4) + workers** — add `animation/{particle-generator,shake}`, `borders`, `transition`, `ktx2-parser`.                                                                                                                  | [ ]                                                        |

**Inherited deferrals this phase OWNS** (full annotations in the [Cross-phase deferrals](#cross-phase-deferrals-live) table):

- **Wall EDGE rename (3→5)** [Batch 5.1]: **DONE** (commit `10677cad7`) — EDGE\_\* consts added + WALL\_\* deprecated; all references flipped; `WallDocument` edge/darkness/initializeEdge added.
- **`shape`/`_shape` derived props (4→5/7)** [Batch 5.6]: author `client/data/shapes.d.mts` (`Rectangle`/`Circle`/`Cone`/`Ellipse`/`Polygon` `ShapeData`), then add the deferred typed `shape` on `tile`/`ambient-light`/`ambient-sound` + `_shape` on `drawing` (inline `// TODO(v14)` in each).

### Batch 5.1 — geometry/ (DONE — commit `10677cad7`)

Verified `[x]` (member-diffed, CI-green, type-tested): edge (rewrite: +darkness, level-keyed intersections, level params, EdgeTypes=wall|source|innerBounds|outerBounds, Edge.ThresholdData) · vertex (EDGE_SENSE/RESTRICTION, drop phantom isTerminal) · collision · observable-transform · unbound-transform · ray · limited-angle-polygon · polygon-mesher · weiler-atherton-clipping. Rename `[x]` quad-tree→quadtree. Add `[x]` shapes/elevated-surface-exposure-polygon (PolygonTree return FIXME `object|null`). Wall EDGE deferral `[x]`.

**`[~]` Level-entangled — finish in Phase 7 (Scene Levels):**

- **edges (CanvasEdges)** — `clear()/inititalize()/refresh()/getEdges` done; constructor `level` param + `get level()` FIXME-typed `object` until the `Level` document exists.
- **clockwise-sweep** — non-Level surface already v14 (`useInnerBounds`, `_determineEdgeTypes` param fixed to `PolygonType`, edgeTypes keyed by v14 EdgeTypes); still needs `Config.level` + the `this.level.id` intersection internals.
- **source-polygon (PointSourcePolygon)** — still missing `get level()`, `get scene()`, `surfaceExposure` (→PolygonTree), and `Config.level`. Not yet touched this batch.
- **`client/data/polygon-tree.d.mts`** — missing file (return type of `elevated-surface-exposure` + edge-tree); author in Phase 7 client/data, then drop the `object|null` FIXMEs.

> **Reminder — the 4 former-Phase-4 giants are NOT in this phase.** active-effect / region / scene / token are reassigned to **Phase 7** and blocked until their prerequisites exist — see the "client/documents Tier-B giant member-diffs" row in the [Cross-phase deferrals](#cross-phase-deferrals-live) table (full per-giant annotations in the archived Phase 4 detail). Don't get pulled back into them.

### Batch 5.2 — rendering/ (DONE — CI green)

**SCOPE FINDING (2026-05-26): v14 refactored the shader/filter base API.** Coupled ~60 files; the base change + all subclass renames landed in one atomic commit (CI cannot be green with a half-done base). 68 src + 37 test files; all six gates green (tsgo default, tsc --eOPT false, raw tsgo, test-types 463✓, eslint, prettier).

- **`BaseShaderMixin`** (`mixins/base-shader-mixin`) gained: `defaultUniforms` (source: getter → **modeled as a property** to avoid accessor/property-override churn across ~36 subclasses), `_createVertexShader(options?)`, `_createFragmentShader(options?)`, `_configure(options?)`, `_fragmentShaderCompatibility(options?)`, `_vertexShaderCompatibility(options?)` — the last two are `protected static` shims (`{since:14, until:16}`). (`create` stays on the concrete bases, matching the repo's existing simplification — source has an abstract no-op `create` on the mixin.)
- **`AbstractBaseShader`/`AbstractBaseFilter`** lost `static vertexShader`/`fragmentShader`/`defaultUniforms` (moved to mixin / replaced by the `_create*` methods); `create` gained a 2nd `options?` param.
- **Subclass recipe** (the dominant per-file change): `static [override] fragmentShader: X` → `protected static override _createFragmentShader(): string`; `static [override] vertexShader: string` → `protected static override _createVertexShader(): string`; `defaultUniforms` stays a property. `_create*` are **protected**, so `.test-d.ts` access them via bracket notation (`Shader["_createFragmentShader"]()`).

**Reusable findings (for 5.3+ and other shader-adjacent work):**

- **Holdout:** `shaders/samplers/amplification` keeps a real `static fragmentShader()` **method** (NOT a property; modeled `@deprecated since v14, until v16` since the base `_fragmentShaderCompatibility` shim routes it). The only file that did not rename to `_createFragmentShader`.
- **Variance hack:** `filters/glow-overlay` source overrides the generic `_createFragmentShader(options)` with positional `(quality, distance)` numbers; modeled as `_createFragmentShader(quality?: AnyObject | number, distance?: number)` (the `AnyObject` keeps it structurally assignable to the base) with an explaining `@remarks`. Genuine upstream signature inconsistency.
- **Deprecation removals done here (so Phase 8 needn't re-find them):** `AdaptiveLightingShader#getDarknessPenalty`, `AbstractBaseShader#_defaults`, `GlowOverlayFilter#animate` (all `@deprecated … until v14`). Also removed v14-gone non-deprecated members: weather `createProgram` + the weather/visibility/outline `create` overrides that no longer exist in source.
- **Phase 8 leftover:** `AbstractBaseShader.FragmentShaderFunction` namespace type is now orphaned (no referent after `fragmentShader` removal) — kept (removing an exported type is a deliberate breaking change); drop in Phase 8 cleanup.
- **Source getters the repo had as fields, now fixed:** `SHADER_HEADER` (coloration/illumination/background/darkness-lighting), `FRAGMENT_UNIFORMS` (base-lighting), `FRAGMENT_HEADER`/`commonUniforms` (base-weather).

**Sub-batch checklist (all DONE & CI-green):**

- **5.2a foundation** `[x]` — base-shader-mixin, fragment-channel-mixin, base-shader, \_types, blend-modes, batching/{batch-renderer (fixed `PreRenderBatchFunction` instance param),batch-shader-generator}, filters/{base-filter,base-mask-filter}.
- **5.2b lighting** `[x]` — base-lighting + coloration/illumination/background/darkness + 26 lighting/effects.
- **5.2c vision + weather** `[x]` — vision/\* (6) + weather/\* (5).
- **5.2d samplers + region + grid + graphics** `[x]` — samplers/\* (10), region/\* (3), grid/grid, graphics/dash-line; `_types` BatchData namespaces verified vs source.
- **5.2e filters** `[x]` — filters/\* (incl smaa/\*; `smaa/weights` class renamed `SMAABWeightCalculationFilter`→`SMAABlendingWeightCalculationFilter`).

---

## How to update this file

**This is the single source of truth for the v14 migration, and the work spans many separate agent sessions. Keeping it current is mandatory, not optional — an out-of-date tracker is how context gets lost between sessions.**

### Per-session protocol (do this every v14 session)

1. **At the start:** read this file. The next work is whatever the roadmap row marks "In progress" / "Not started" and whatever per-file box is still `[ ]` or `[~]` in the active phase. Also scan the **[Cross-phase deferrals](#cross-phase-deferrals-live)** table for anything your phase inherits. Don't re-derive the plan from scratch — trust and extend what's here. (Closed-phase specifics, if needed, are in [migration-v14-archive.md](migration-v14-archive.md).)
2. **As you work:** when you discover scope that wasn't anticipated (as the `Temporary` discovery reshaped Phases 1/2, and the Scene Levels discovery reshaped Phase 3/7), record it immediately — fix the roadmap, note why, and assign it to the right phase (add a row to the deferrals table if it crosses phases) so it is never silently dropped.
3. **Before you finish (and before each commit):** sync **all views** so they never disagree:
   - the **roadmap status row** for the phase,
   - the **per-file checkboxes** (`[x]` done & committed, `[~]` partial — add a note, `[ ]` not started),
   - the **[Cross-phase deferrals](#cross-phase-deferrals-live)** table (anything you deferred or actioned),
   - the **running notes** in the active phase's detail (commits landed, remaining items, type-system gotchas worth reusing).

### Maintenance rules

- Check a box only when a file meets all three done-bar criteria (source-diffed, CI-green, type-tested); use `[~]` with a note for partial work.
- When starting a phase, expand it into a per-file checklist like the archived Phase 1/3.
- Record the v14 source build you diffed against if it changes (currently 14.363.0).
- Record cross-phase deferrals in **both** the [Cross-phase deferrals](#cross-phase-deferrals-live) table (with receiving phase) **and** the receiving phase's roadmap row, so neither side loses them.
- **When a phase closes (CI-green & committed), move its expanded detail to [migration-v14-archive.md](migration-v14-archive.md)** and leave a one-line pointer under [Closed-phase detail](#closed-phase-detail-archived). Lift any cross-phase deferrals out of the detail into the live table _before_ archiving, so they stay loaded. This keeps the always-loaded tracker small (it is `@`-imported into every session — keep it well under ~40k chars); the archive is **not** `@`-imported. Nothing is deleted — archiving is a move, the commit message links the work.
